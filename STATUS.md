# מצב הפרויקט — נקודת המשך

עודכן: 07.09.2026

קובץ קצר שנועד לדבר אחד: להתחיל שיחה חדשה — במחשב אחר, בנייד, או אחרי
הפסקה — בלי לאבד את החוט. **הכללים עצמם נמצאים ב-`CLAUDE.md`, וקריאתו
קודמת לכל עבודה כאן.**

---

## מה נסגר לאחרונה

| נושא | מצב |
|---|---|
| כפילות בטיסות (`FCO` מול `ROME FIUMICINO`) | נסגר — `airportKey` ב-`bookingIdentity` |
| מיקומים ומפת היום | נסגר — כתובות מנוקות ב-`placeLookupService.addressVariants` |
| עריכת מסלול יומי (תיקון, הוספה, הזזה) | נסגר — שכבת `overrides`, חצים ↑↓ |
| קורלציה בין פרטי הנסיעה לתכנון | נסגר — `tripAnchorsService` + `DayAnchors` |
| תאריך ויום בכל יום בתכנון | נסגר |
| נקודת כניסה אחת להוספת פריט | נסגר |
| קישור לאישור המקורי במייל | נסגר — `messageId` נשמר, ואומת חי: 12 מ-13 הרשומות נושאות אותו והקישור מוצג |
| נסיעה שהסתיימה נשארה תחת "הנסיעות שלך" | נסגר — יורדת לקודמות ביום שאחרי |
| תמונת מקום שגויה תחת שם נכון | נסגר — `mustBePlace` ב-`placeMediaService`: פוסל דף פירושונים, ערך בלי קואורדינטות שהגיע מחיפוש, ולוגו |
| כותרת כהה על רקע כהה | נסגר — הוסר `color` מכלל `h1..h6` הגלובלי ב-`theme.css`; ארבעה מקרים בעמוד אחד |
| כפתור "אתר" באטרקציות + 66 אתרים חדשים | נסגר — אותו מקור כמו במסעדות (OSM), מ-3 ל-9 אטרקציות ליעד |
| שובר טרקלין בנה נסיעה בשם "יעד לא ידוע" | נסגר — קבוצה בלי מקום אינה מגדירה נסיעה (`tripGroupingService`) |
| "הנסיעות שלך (1)" ספרה פוליסה שפגה | נסגר — הספירה מונה נסיעות בלבד; אין נסיעה ⟵ "פריטים שדורשים טיפול" |
| כתובת השולח נזרקה לפני הפענוח | נסגר — `senderIdentityService`, ו-`sourceFrom` על כל רשומה |
| "הבא בתור" קרא הזמנות בלבד | נסגר — `plannedTimelineService`; **טרם נצפה על מסך אמיתי**, ראה פתוח 2 |
| "כפילות" הטיסות — הלופ שחזר שוב ושוב | נסגר — לא כפילות אלא ערך הפוך בפענוח. `bookingConsistencyService` בודק קוהרנטיות ולא נוכחות |
| ממשק מתורגם בפריסת RTL בכל השפות | נסגר — `dir`/`lang` הוחלו רק בהחלפת שפה, לא בטעינה; ו-`index.css` כפה `direction: rtl`. אומת: fr⟵ltr, he⟵rtl ללא רגרסיה |
| שלושה קישורי ניווט קשיחים בעברית מתוך 12 | נסגר — `nav.rollingTrip/journal/matchmaker` + "עוד" + תווית המשתמש, בחמש שפות |
| שמות היעדים בדף הבית בעברית בכל השפה | נסגר — `label` הופרד מ-`name` ב-`PlaceImage`; החיפוש נשאר עברי, 6/6 תמונות |
| שגיאת כתיב "מצ'קמייקר" | נסגר — `מאצ'מייקר`, אפס שאריות בעץ |
| `/trip-map` — 680 שורות נתוני סימולציה בלי קישור מאף מקום | נמחק. שני הרעיונות ששרדו — לוויין ופאנל עצירות — עברו למפה האמיתית |
| כותרת ה-Hero הבטיחה הדגמה שלא מרונדרת | נסגר — `titleGeneric` (קיים ומתורגם בחמש שפות, לא נקרא מעולם) מוצג כשיש נסיעה |
| ניגודיות ה-Hero נכשלה, אחרי שההדר תוקן ולא הוא | נסגר — `#667eea`⟵`#5568d3` ו-`#f093fb`⟵`#8e3fa8`. נמדד על המסך: הכותרת 3.66 ⟵ **5.19:1** |
| שתי אנימציות אינסופיות ב-`DemoItinerary` בלי שומר | נסגר — אומת בגיליונות: החץ וכפתור ההדגמה עוצרים ב-`prefers-reduced-motion` |
| שני כפתורים בדף הבית שאינם שייכים לו | נסגר — "טיול קבוצתי" היה כפילות של הסרגל + חומת התחברות לאורח; "מה לארוז" נפתח בלי ולו prop אחד |
| `/destination-info` — 482 שורות עברית, "לא מתורגם" | נסגר — הסקירה הראתה ש-385 מהן **מסד יעדים** ורק 16 ממשק. כל ה-16 + 11 שמות ערים; אפס עברית בצרפתית |
| הסרגל התחתון בנייד — 2 מתוך 5 פריטים קשיחים בעברית | נסגר — `BottomNav` לא נבדק עם הסרגל העליון. אותם מפתחות `nav.*`, לא עותק שני |
| תת-הכותרת החדשה הציפה את הנייד | נסגר — נוסח קצר ב-`xs`, גזור מהמלא. הצצה ‎-103 ⟵ ‎-32px, הכפתור עלה מ-531 ל-462 |
| אותו יעד נשא שני שמות: "פריז" בכרטיס, "Paris" בחלונית | נסגר — `label` מתורגם, `destination` נשאר אנגלי כי הוא הפרמטר בקישור |
| מצב כהה: 28 אלמנטים של לבן על לבן בדף הבית | נסגר — רקע קשיח + `color` מהערכה. נמדד: 28 כשלים ⟵ **0**, בשני המצבים |
| כותרת ה-Hero הוחלפה בהחלטת המשתמש, ותת-הכותרת הוחזרה | נסגר בדסקטופ — הנוסח החדש דחף את ה-Hero מעבר לקיפול (הצצה ‎-12px); אחרי קיצוץ גופן וריפוד: **66px**. **בנייד עדיין ‎-103px** |
| אין `h1` בדף | נסגר — `component="h1"` |
| תאריכי ISO גולמיים בכרטיס הנסיעה | נסגר — `fmtRange` לפי `i18n.language`, עם נפילה לערך הגולמי כשאינו ISO |
| כפתור שיתוף בתוך כרטיס לחיץ — Enter פתח דיאלוג **וגם** ניווט | נסגר — הופרדו לאחים; הניווט עבר ל-`<button>` אמיתי במקום `div[role=button]` עם `onKeyDown` ידני |
| ניווט מקלדת בלי סימון פוקוס בכל האפליקציה | נסגר — `index.css` הסיר `outline` מכל כפתור, ובכל העץ היו שני `:focus-visible`. נוספה טבעת גלובלית `#5568d3`; אומת בטאב אמיתי |
| 54 כפתורים ב-26 קבצים רונדרו לא כפי שנכתבו | נסגר — `style.css` דרס `sx` עם `!important` על `.MuiButton-containedPrimary`. ארבעת כללי ה-MUI שם נמחקו; `theme.js` כבר מגדיר את כולם, ושם הם ניתנים לדריסה |
| המפה הראתה יום אחד בלבד — נסיעה בין ערים לא נראתה כמסלול בשום מסך | נסגר — מצב "כל הנסיעה" ב-`TripMap`, מאותו `dailyItinerary`. אומת: פריז⟵בורדו, 6 סיכות ימים, 5 קטעים, 17 נקודות |
| אות פופולריות בעמוד החיפוש — הלולאה מתה אחרי 5 מקומות | נסגר — 429 מוויקיפדיה. בקשה אחת מרוכזת במקום 2 לכל מקום; 40 מקומות ב-362ms |
| ערך שגוי שנראה נכון: `"Pantheon"` ⟵ הפנתיאון של **פריז** | נסגר — `titles=`+`redirects=1` במקום `list=search`; ו-`coordinates` פוסל דף פירושונים ומונח גנרי |
| שתי תכונות בדף הבית לא עברו תרגום | נסגר — `home.features` הכיל 5 מפתחות מול 7 כרטיסים; 81 מפתחות נבדקו בחמש שפות |
| אטרקציה ריקה לגמרי נשמרה במאגר | נסגר — `activity` נוסף ל-`PURGEABLE` בהחלטת המשתמש; רשומה עם מספר אישור אינה נמחקת לעולם |
| אריחי מפה במצב לא־מקוון | נסגר — מטמון אריחים ב-`sw.js`, תקרת 500. אומת חי: אריח שנצפה מוגש עם `transferSize: 0` |
| הפירוט היומי היה תוכן ממלא מקום | נסגר — `generateItinerary` מזין את דף הבית ואת הטיול המתגלגל; 633 שורות של מערכים קשיחים ירדו |
| "מרחק: לא זמין" מעל מפה שמראה 347 ק"מ | נסגר — `roadRouteService` עם OSRM; נמדד 342 מול 347 של גוגל |
| ציון "התאמה לטיול" סתר את הטקסט לידו | נסגר — הציון הוסר; "מזג אוויר מושלם" מותנה בכך שאין ולו אזהרה אחת |
| שלוש לשוניות ריקות בכל יעד מובנה | נסגר — השלמה ברקע מאותו מקור AI, המידע הידני גובר, והחלק מסומן כאוטומטי |
| הוספת תמונה ביומן — גרירה והדבקה | נסגר — `addPhotoFiles` היא נקודת כניסה אחת לשלוש הדרכים (בחירה, גרירה, ⌘V); `resizeBase64` מחזירה `null` במקום להישאר תלויה על קובץ שאינו מפוענח (HEIC), וההודעה נגזרת ממה שקרה בפועל. אומת על המסך: 0→1 בהדבקה, קובץ טקסט נדחה, 7 תמונות מול מקום ל-4 נתנו "נוספו 4 · 3 מעבר לתקרה", ו-5 תמונות נשמרו ל-`journal_entries` |
| "כפתור התמונה ביומן המסע לא נותן לבחור" | **לא היה באג.** נמדד: הלחיצה מגיעה ל-`input` (`inputClicks:1`), אין כיסוי (`elementFromPoint` = הכפתור), והצנרת מייצרת `data:image/jpeg`. במאק התמונות סגורות בתוך `Photos Library`, שחלון בחירת קבצים אינו נכנס אליה — לכן נפתחת תיקייה ריקה למראה. הדלת היחידה היא **"מדיה ← תמונות"** בסרגל הצדדי. אומת: התמונה נוספה |
| שלושה כשלים שקטים | נסגר — כולם דפוס מס' 1, תוקן בסוג אחד ולא באחיו. **א׳** (`f3bbecb`): פענוח שנכשל נשמר במטמון כתוצאה ריקה ל-24 שעות; האח `aiAttractionsService` כבר זרק `INVALID_RESPONSE`. **ג׳** (`9573fe6`): `cloudError` יושם בשניים מחמישה נתיבי כתיבה; שלושת הנותרים חוברו, 4←7. **ב׳**: שגיאת פענוח הוצגה כ"נסרק ולא זוהו פרטי הזמנה" — זהה למייל שאין בו הזמנה; עכשיו מכסה, 5xx ו-`PARSE_FAILED` נאמרים בשמם, וכל ניסוח מסתיים ב"נסה שוב" כי כשל הוא "לא בדקנו" ולא "אין כאן" |
| מסלול על מפה אמיתית, לא iframe | נסגר — `RouteOnMap` מזין `path` מ-OSRM ל-`InteractiveMap` כ-Polyline עם סמנים. בדרך תוקנו שלושה: המפה ציירה רק באתחול ולכן מסלול שנשלף מהרשת לא הופיע; סמן בלי קטגוריה סוּנן בשקט; וזום קבוע (7) הבליע מסלול של 66 ק"מ. נמדד חי: תל אביב→ירושלים, 299 נקודות, זום 10 |
| המפה נכשלה בפרודקשן | נסגר — `RefererNotAllowedMapError`. המפתח ששימש היה **יתום**: לא נמצא באף אחד מחמשת הפרויקטים ב-Google Cloud. הוחלף ב-`Maps Platform API Key` שבפרויקט `Travel-Companion-Maps`, עם דומיינים מפורשים. **הלקח: הקונסולה באתר החי אמרה את הסיבה בשורה אחת, ואליה הגעתי אחרון** — שלושה סיבובים בוזבזו על `key=''` שמקורו ב-`RouteMap`, קוד מת שאיש אינו מרנדר |
| "הבא בתור" — נצפה על מסך אמיתי | נסגר — נזרעה נסיעה קרובה עם תוכנית פעילה ב-`/travel-info`. נמצא ותוקן באג: `nextFromBoth` מיין לפי `at`, וכניסה למלון (חצות) הקדימה טיסת 06:20. `DAY_ANCHOR` נותן לה 1441, אך `order` היה שובר-שוויון בלבד. אומת בשני הכיוונים |
| ניווט יצא מהמיקום הנוכחי במקום מנקודת המסלול | נסגר — `location=` אינו פרמטר ב-Maps URLs API; גוגל בולעת אותו ונופלת למיקום המכשיר. נוסף "נווט את כל המסלול" עם `origin` מפורש. נמדד: פירנצה→רומא 274 ק"מ במקום 4,102 ק"מ דרך טורקיה |
| האטרקציות והמסננים חזרו למפה (שלב 3) | נסגר — `AttractionsPanel` חובר ל-`/map` במקום ה-`iframe` של 25.2.2026. הפאנל מעולם לא רץ, וכשהורץ נמצאו חמישה כשלים: `useEffect` ירה לפני שסקריפט Maps נטען ולא ניסה שוב לעולם (זו הסיבה היחידה שנראה מת), כשל דווח כ"✅ נמצאו 0", קולבק שאינו נקרא השאיר ספינר נצחי, ותמונה ודירוג הונחו כקיימים — בקניות בפירנצה הם חסרים ב-10 ו-8 מתוך 20. `PlacesService` הישן **נבדק ועובד** עם המפתח הזה: 15 צירופים (3 ערים × 5 קטגוריות), כולם `OK`, ו-0 מתוך 150 תוצאות מסוג שגוי |
| "יש גרסה חדשה" נדבק לנצח ב-`localhost` | נסגר — `public/version.json` נכתב בידי craco בכל עלייה, ושרת פיתוח ובנייה לפרודקשן כותבים לאותו קובץ. `npm run build` בזמן ש-`npm start` רץ החליף את החותמת המוגשת בעוד ה-bundle בדפדפן נושא את הישנה. נמדד: `14:42:47` בקוד מול `16:12:02` ב-`/version.json`. בפיתוח הבדיקה כבויה — HMR ממילא מחליף את הקוד — ואומת מה-bundle הבנוי ש-`isDev` מקופל ל-`!1` ושהבדיקה החיה שלמה |
| 17 מסנני הקטגוריות עובדים שוב | נסגר — הסיבה בקוד: `onMapLoad` מוצהר ב-`App.js` ואינו נקרא מאף מקום. הוא היה היחיד שהציב `mapRef.current` ו-`isMapsLoaded`, וכשהמפה הוחלפה ב-`iframe` שניהם קפאו — כל צרכני `activeFilters` יוצאים בשומר שלהם. הקטגוריות עברו ל-`placeCategories.js` (מקור אמת אחד; ב-`App.js` הן הופיעו שלוש פעמים) ולפאנל ב-`/map`, כארבע קבוצות שהמשתמש עצמו חילק. נמדד בתל אביב: 50 · 79 · 96 · 79 שורות לארבע הקבוצות, כיבוי קטגוריה 79⟵20, כיבוי הכל ⟵ "לא נבחרה אף קטגוריה" |
| הבלוק הישן בדף הבית הוסר | נסגר — `App.js` מ-1,517 ל-121 שורות. ירדו 402 שורות JSX, שתים-עשרה פונקציות מתות (`searchRoute` לבדה 196 שורות), `useEffect` של הסמנים, 24 קריאות `useState`, `CATEGORY_ICONS`, וכ-60 שורות **JSX חשוף מחוץ לכל `return`** — הוראות סתמיות שהתפרשו ונזרקו, אך החזיקו ייבואים חיים ולכן נראו בשימוש. נמדד על דף הבית לפני ואחרי: 6,628px⟵3,013px, 71⟵28 כפתורים, 9⟵1 שדות, iframe אחד⟵0. שישה נתיבים נבדקו אחרי ההסרה, כולם מרנדרים, קונסולה נקייה |
| מזג האוויר היה מומצא | נסגר — שני משטחים הציגו קבוע: `weatherAPI.js` היה **סימולציה מוצהרת** שהחזירה 22° אחרי `setTimeout(500)` (השהיה מזויפת שגרמה לזה להיראות כקריאת רשת), ו-`DestinationInfoPage` נשא `{temperature: 22, description: 'בהיר'}` קשיח. נמדד על המסך: רומא, לונדון ובנגקוק הראו **22°C, בהיר** — זהה. שניהם עברו ל-`openMeteoService` (בלי מפתח), ונמדד אחרי: 26° · 19° · 26°. `weatherAPI.js` נמחק |
| מפתח OpenWeather — הפריט נסגר אחרת מהצפוי | שני המפתחות ב-`.env` **תקינים** (HTTP 200, ומבינים שמות עבריים). אבל סריקת 44 הצ'אנקים של האתר החי מצאה **אפס `appid=`** — כלומר לא הייתה בפרודקשן ולו קריאה אחת ל-OpenWeather. מה שהיה שם הוא הקבועים הקשיחים בלבד. Open-Meteo, שאינו דורש מפתח, כבר היה שם |
| תחזית 7 הימים ב-`/trip-planner` | נסגר — `getMockWeatherData()` השתמש ב-`Math.random()`: טמפרטורה 20–29 ומצב אקראי מארבעה, כלומר תחזית שהשתנתה בכל לחיצה. הוחלף ב-`fetchTripWeather`. בדרך נמצאו שלושה באגים שהיו בשירות מלכתחילה: `startDate` נשלח כאובייקט `Date` גולמי; `start_date` נשלח בלי `end_date` (400 — "must have the same number of elements") ו-`forecast_days` התנגש בו; ותאריך נבנה דרך UTC. **גם `getStopWeatherSummary` סבל מהם**, כלומר תחזית העצירות ב-`/rolling-trip` החזירה `null` בשקט |
| `ChunkLoadError` הרג את כל המסך | נסגר — כל דף הוא `React.lazy`, ולכן אחרי כל פריסה מי שהשאיר טאב פתוח מבקש צ'אנק בשם שכבר אינו קיים. `ErrorBoundary` הציג לו "Loading chunk ... failed" באנגלית ו"נסה שוב" ש**אינו יכול להצליח** — הוא רק מרנדר מחדש ומבקש את אותו קובץ. עכשיו: זיהוי `ChunkLoadError`, רענון יחיד עם דגל `sessionStorage` נגד לולאה, והודעה בעברית. אומת על מסך: רענון אחד ואז עצירה; שגיאה רגילה אינה נוגעת בזה |
| כלי Claude Code לפרויקט הותקנו | נסגר — MCP: `context7` (תיעוד ספריות חי, משרת את כלל "מחקר לפני פעולה") ו-`firebase` (קריאה ואימות של `firestore.rules`, ההגנה האמיתית היחידה); שניהם `✔ Connected`. סקילים: `/verify-live` (סורק 45 צ'אנקים, מפענח `\uXXXX`, עם `--self-test` דו-כיווני) ו-`/seed-bookings` (8 פיקסצ'רים בצורת הרשומה האמיתית). הוקים: אימות i18n אחרי עריכה, וחסימת `.env`. סוכן: `fabricated-data-hunter`. פקודה: `/deploy-check` |
| שני מפתחות OpenWeather ב-`.env` | נסגר — שתי השרשרות נמדדו מתות עד הסוף, ו-**שלושה** רכיבי מזג אוויר נמצאו במקום שניים. הוסרו: `travel-info/WeatherForecast.js` (559), `weatherService.js` (340), `maps/WeatherWidget.jsx` (346) והבלוק ב-`TripItineraryView`. `weather` ירד מ-`API_KEYS`, מ-`validateApiKeys` ומ-`API_ENDPOINTS` — **וגם משני ה-interfaces ב-`types/index.ts`, שבהם הוא היה שדה חובה**; הסרה מהאובייקט בלבד הייתה שוברת את הטיפוסים. אומת: build עבר, אפס הפניות תלויות באוויר, `/map` מצייר (`mapDrawn: true`, `innerWidth: 1280`) ו-`/travel-info` מרנדר בלי שגיאה חדשה. **הגינות: 0 `appid=` ב-45 הצ'אנקים אינה הוכחה — היא הייתה 0 גם לפני, כי הקוד כבר היה מת.** |
| `REACT_APP_OPENAI_API_KEY` — מפתח שלישי מת | נסגר — `API_KEYS.openai` היה **קבוע `''`**, ולכן `validateApiKeys` דיווח "Gemini חסר" תמיד; הפונקציה ממילא אינה נקראת. נמדד באתר החי: 0 מ-45 צ'אנקים מכילים `api.openai.com`. `openaiService.js` נשאר — **שמו מטעה, הוא קורא ל-`geminiEndpoint`** ואין בו מפתח. הביטול אצל OpenAI ומחיקה מ-Vercel בידי המשתמש |
| **ששה מתוך שבעה endpoints היו פתוחים לאינטרנט** | נסגר — נמדד חי: `POST` אנונימי ל-`/api/gemini` החזיר תשובה מלאה, `GET` ל-`/api/place-website` ביצע בקשה בתשלום, ו-`/api/notify-check` החזיר `projectId`, `users`, `flights`. המפתחות אכן עברו לשרת — אבל ה-endpoint שמחזיק אותם נשאר פתוח, ולכן המפתח לא נגנב אלא **הושאל**. דפוס מס' 1 שוב: `verifyIdToken` יושם ב-`push-test` בלבד, `CRON_SECRET` ב-`check-flights` בלבד. **אימות זהות היה שובר את האתר** — נמדד ש-Gemini נקרא מ-8 מסכים ציבוריים — ולכן נבנה `api/_lib/guard.mjs`, חסם מקור שבודק `Origin` **וגם** `Referer` (דפדפן אינו שולח `Origin` ב-GET מאותו מקור). אומת בשני הכיוונים על האתר החי: `curl`⟵403, דפדפן אמיתי⟵200. |
| תלויות צד-שלישי ו-OAuth | נסגר — `npm audit` החזיר 66, אבל המדידה שקבעה הייתה מי מהן **נארזת**. `jspdf` (CVSS 9.6) היה מיובא ב-`TripPlanner` עם **אפס שימושים** ובכל זאת נארז — ייבוא לבדו מספיק ל-webpack. `axios` היה תלות ישירה עם אפס אזכורים (28 ייעוצים). `react-router-dom` שודרג 7.13⟵7.18.3 בגלל open redirect ב-`<Link>`. 66⟵60, bundle 21MB⟵18MB. הנותרות אינן מגיעות לדפדפן: `websocket-driver` הקריטי מגיע מ-`@firebase/database` ש-tree-shaking משמיט — 0 מ-43 צ'אנקים. **OAuth נקי:** `gmail.readonly` בלבד, האסימון נשלח רק ל-`gmail.googleapis.com` ואינו נשמר כלל. |
| דף הבית — ששת הפריטים הראשונים מהביקורת | נסגר — נמדד לפני: **אפס** הפניות לנתוני המשתמש ב-`HomePage.js`, טענת "מעל 10,000 יעדים" מול 69 רשומות במאגר, 938px גלילה עד תוכן שימושי, 19 מתוך 35 יעדי מגע קטנים מ-44px, ותמונה **אחת** בכל הדף. נמדד אחרי: הדף 3488px⟵2269px (4.3⟵2.9 מסכים), יעדי מגע קטנים 19⟵6, שש תמונות ויקיפדיה אמיתיות. `NextUpCard` **לא שוכפל** אלא נקרא מ-`travel-info` — אותה פונקציה בשני המסכים. **הוא אינו נראה כרגע**, ובצדק: אין נסיעה עתידית בטווח, ואותה התנהגות נצפתה גם ב-`/travel-info`. |
| הזמנות מסעדה — מהמייל אל היום בתכנון | נסגר — שני שלבים. שלב 1 הוסיף את הסוג לפרומפט של `parseBookingEmail`, **שנקראת ממקום אחד בלבד: תיבת ההדבקה הידנית**. הסריקה החיה עוברת ב-`buildExtractionPrompt`, ושם עמדה ההוראה ההפוכה — "if the booking is for a restaurant table, set isBooking to false" — כלומר הצינור דחה במפורש כל מסעדה. שלב 2 מצא שמונה שומרים ולא שלושה; החשוב בהם `eventsFor`, שבו `restaurant` נפל ל-`default` ("ביטוח ומה שאין לו רגע בזמן") ולכן הרשומה נשמרה ולא הופיעה בשום יום. אומת בשתי הרסות על הצינור האמיתי, ואחת מהן הורצה מול הקוד שלפני השינוי והחזירה 0 אירועים. אומת גם על המסך עם `localStorage` מוזרע. **לא אומת:** הפענוח מול Gemini — דורש סריקה חיה |
| ארבעה באגים שנמצאו באימות מול התיבה האמיתית | נסגר — 05.09.2026, בהרשאת המשתמש. הפענוח עצמו עבד: גוגל ריזרב, TheFork ו-Tabit הוחזרו נכון, ושני ניוזלטרים נדחו. **(א)** Tabit החזיר `2024-08-16` במקום `2025`: הגוף כותב "16/8" בלי שנה, ו-`gmailService` שולף את תאריך המייל בעוד `bookingScanService` אינו מעביר אותו — בדיוק כמו `messageId` ו-`sourceFrom`. **(ב)** `EmailImportModal` החזיק מיפוי משלו ובו טיסות, רכב ומלון בלבד; אטרקציות, ביטוח ומסעדות נזרקו בשקט והמסך אמר "נמצאו ויובאו: ." — הצלחה עם רשימה ריקה. **(ג)** אותו שולחן בנאפולי הוזמן בשני ספקים והוצג פעמיים; הסיבה **אינה** הפסיק (`nameKey` מנקה אותו) אלא `startsWith` ב-`sameName` — הליבה יושבת בסוף כשהספק מקדים את סוג העסק. **(ד)** חמישה שולחים נוספו/הוחרגו, ו-Google Reserve חייב טבלת כתובות: `google.com` שולח גם התראות אבטחה וקבלות. אומת לפני/אחרי על אותו מייל: `2024-08-16`⟵`2025-08-16`, ו"נמצאו ויובאו: ."⟵"1 מסעדות" |
| סריקה חיה החזירה 15 מיילים ואפס הזמנות | נסגר — **היומן, לא הפענוח.** `PARSER_VERSION` עמד על 3 מאז schema.org, בעוד ההנחיות שוּנו שלוש פעמים (שלב 1, שלב 2, ותיקוני 05.09). `scanLedgerService` מדלג על כל מייל שכבר פוענח באותה גרסה, ולכן אישורי המסעדות שיושבים בתיבה לא הובאו כלל. אומת שהמנגנון חל: שלוש שורות הנושא עוברות את מסנן המילים, כלומר השאילתה **הייתה** מביאה אותן — ובכל זאת נסרקו 15 מתוך תקרה של 60. הועלה ל-4. **בדרך התגלה:** חמישה מהמיילים היו התראות TripIt שמצטטות את שורת הנושא של האישור האמיתי בלי פרטים — מצע לערך מומצא. `isEntitlementSender` הוכלל ל-`neverBookingSender`; לקרוא ל-TripIt "benefit" היה שקר בשם השדה. **טרם אומת:** שהסריקה הבאה מחזירה את המסעדות |
| סריקה שנפלה כולה בגלל מייל אחד | נסגר — הפענוח היה עטוף ב-`try`, אך הפסילה לפני המודל, בניית הרשומות והדחיפה לרשימה לא. חריגה אחת הפילה 60 מיילים והציגה שגיאה במקום תוצאה. גוף הלולאה עטוף כולו, והנופל נרשם ב-`unrecognized` ולא נבלע. אומת בהרסה התנהגותית: `gmailService` מוזרק דרך הוק של ה-loader, שלושה מיילים נכנסים ללולאה האמיתית ואחד זורק — לפני: קריסה מלאה; אחרי: `parsed=2`, שתי הזמנות, והנופל מדווח. **הסריקה החיה אחריה: 58 מיילים, 28 אישורים** (לפני: 15 ואפס) |
| שלושה באגים במסך שנמצאו כשהמשתמש ביקש "תבדוק" | נסגר — כולם על מסך אמיתי אחרי סריקה מוצלחת. **(א)** `gapMinutes` חישב מרגע ההמראה; שעת הנחיתה ישבה ב-`extra` ולא נכנסה. LY5111 15:00⟶17:15 הציגה "5 שעות ו-30 דקות" עד ארוחה ב-20:30 במקום 3:15 — וכך **כל** פער שמופיע אחרי טיסה. נוסף `freeAt`; כשהנחיתה אינה ידועה או חוצה חצות לא מוצג פער כלל. **(ב)** המודל החזיר את שם העסק בתוך שדה הכתובת, והשורה חזרה על הכותרת. ההסרה בתצוגה בלבד — הערך השמור נשאר מלא לאיתור על המפה. **(ג)** המפריד "יום אחד ללא הזמנות" נעלם: `skipped` חושב על `day.date` שהוא רגע האירוע הראשון ולא היום — 33 שעות התעגלו ל-1. הטעות דו-כיוונית (2.96 יממות ⟵ "יומיים"). עבר ל-`dayKey` עם `Date.UTC`. אומת לפני/אחרי על המסך בשני הכיוונים |
| בסיס הנתונים היה בלי גיבוי ובלי הגנת מחיקה | נסגר — נמדד 05.09.2026: `backupSchedules` ריק, PITR כבוי, delete protection כבוי, ושמירת גרסאות שעה אחת. כלומר מחיקה או קלקול = אין ממה לשחזר. **הופעלו:** `DELETE_PROTECTION_ENABLED` וגיבוי יומי בשמירת 7 ימים, שניהם אומתו בשאילתה חוזרת. **התיקון להנחה שלי:** חשבתי שדרוש שדרוג ל-Blaze — הפרויקט כבר עליו מ-02.03.2026; ה-`freeTier: true` הוא מאפיין של בסיס הנתונים (זכאות למכסת חינם) ולא של הפרויקט. הנפח: משתמש אחד, 14 הזמנות, 2 טיולים משותפים, פחות ממגה-בייט — עלות הגיבוי ~2 סנט לחודש לפי `$0.288` לג'יגה-חודש |
| ₪254 לחודש מ-Places Photo | נסגר — הדוח החי נקרא ב-05.09.2026: ₪48.55 בארבעה ימים, תחזית ₪253.98, ומתוכם **₪45.07 = 100% SKU בשם Places Photo** בפרויקט `Travel-Companion-Maps`, מסומן "New". המנגנון: `formatPlaces` ייצר כתובת תמונה לכל מקום ו-`AttractionsPanel` רינדר אווטאר בכל שורה — 4 קטגוריות × ~20 שורות = ~80 בקשות מחויבות בכל פתיחת מפה, בלי מטמון. **וזו התכונה שתיקנו**: הפאנל "מעולם לא רץ", ומרגע שחובר הוא רץ. תוך כדי התגלה ש-`photos` ו-`reviews` נשלפים בתשלום ב-`getPlaceDetails` ו**אינם נצרכים בשום מסך** — `PlaceGallery` יתום. התיעוד דו-משמעי לגבי רגע החיוב, ולכן הוסרו גם הרינדור וגם בקשת השדה |
| דף הבית לא ידע שיש לך נסיעה | נסגר — הפריט "אפס הפניות לנתוני המשתמש" היה מסומן נסגר, **ולא היה**: `NextUpCard` מחזיר `null` מחוץ ל-48 שעות (`HORIZON_MS`), ולכן נזרעה טיסה בעוד 35 ימים והדף לא השתנה בבית אחד (3,170px⟵3,186px, רעש). נוסף `UpcomingTripCard` עם אופק אחר — כל עוד הנסיעה לא הסתיימה — ובו ספירה לאחור, יעד, מה כבר סגור, וכמה ימים בלי תוכנית. **כל מספר נגזר; "חסרה טיסת חזור" ו"אין ביטוח" נדחו במכוון** — כיוון אחד הוא בחירה, ואזהרה שגויה אחת מלמדת להתעלם מכולן. וכשיש נסיעה קרובה הכותרת השיווקית מתכווצת (החיפוש נשאר). אומת: **אורח 3,170px⟵3,170px זהה בדיוק**, ועם נסיעה 3,186⟵3,226 — כרטיס מלא בעלות של 40px |
| האתר החי לא עלה — `Unexpected token '<'` | נסגר — ה-SW שמר `index.html` תחת כתובת של קובץ JS. **השרת היה תקין**: הצ'אנק החזיר 200, `application/javascript`, 1.36MB תקינים, וה-buildTime היה אחרי הקומיט. המנגנון: בזמן פריסה ה-SW הישן מבקש צ'אנק מהבנייה הקודמת, Vercel כבר החליף, והנתיב החסר מוחזר כ-`index.html` **עם סטטוס 200** (SPA rewrite). השומר היחיד היה `if (response.ok)` — ו-200 עובר אותו. מאותו רגע Cache First מגיש HTML לנצח, **ורענון אינו עוזר כי הוא עצמו עובר במטמון**. תוקן בשלושה מקומות: תשובת HTML לבקשת `script`/`style` אינה נשמרת ואינה מוגשת (504 מפורש), גם רשומה שכבר במטמון נבדקת, ו-`CACHE_NAME` v3⟵v4 כדי לנקות הרעלה קיימת. אומת חי: `trip-planner-v4` והשומר מופיעים ב-`sw.js` שעל השרת |
| דף הבית הסביר ולא הראה | נסגר — מבקר ראשון לא יכול היה להבדיל בין האפליקציה לכל מתכנן טיולים אחר, **והדבר שמייחד אותה — בניית המסלול מהמיילים — לא הופיע בדף כלל.** נוסף `DemoItinerary`: ארבעה "מיילים" מצד אחד, והמסלול שנבנה מהם מצד שני, **על המנוע האמיתי** (`buildTimeline` + `humanGap`) ולכן מציג "3 שעות ו-15 דקות" — הפער שתוקן באותו יום. הנתונים מסומנים כדוגמה בגוף הרכיב ואינם נוגעים במאגר. הכותרת הוחלפה בחמש שפות ל"ארבעה מיילים נכנסו. מסלול אחד יצא", והישנה נשמרה כ-`titleGeneric`. **שלוש איטרציות עד שזה תפס את העין:** מכווצת מתחת ל-Hero (נכשל — המשתמש דיווח שגם מי שמכיר איתר אותה רק אחרי כמה שניות), פתוחה בתוך ה-Hero, ולבסוף המיילים גלויים והלחיצה **בונה** את המסלול — כי הרגע שבו זה קורה מרשים מהתוצאה. ונסגר הפער האחרון: **"עכשיו בוא נעשה את זה עם התיבה שלך"** ⟵ `/travel-info?import=1` שפותח את חלונית הייבוא מעצמה. אומת: אורח 3,170px⟵3,209px, הכפתור מעל הקיפול, ואפס גלילה אופקית בנייד |
| שירותי תמונות מתים, ומספרים מומצאים בעמוד החיפוש | נסגר — המשתמש צילם כרטיסי אטרקציות עם תמונה שבורה. `source.unsplash.com` מחזיר **HTTP 503** (השירות נסגר), ו-`AdvancedSearchPage` בנה את שדה התמונה ממנו **תמיד** — כלומר כל כרטיס, תמיד. `picsum.photos` דווקא **חי (200)** ומגיש תצלום אקראי, וזה מסוכן יותר: הוא נראה כמו תצלום של המקום. נוקו כל ההפניות בקוד החי (14). **וחמור מכך נמצא בדרך:** `reviews: 1000 + i * 347` — מספרי הביקורות היו נוסחה על האינדקס, ושני המספרים בצילום (1,000 ו-1,347) הם שני האיברים הראשונים שלה. הדירוגים היו ברירות מחדל קשיחות. הוסרו משלושת הסוגים, **ובדרך נתפסה קריסה שהתיקון עצמו גרם**: `result.reviews.toLocaleString()` על שדה שנעלם, ומסנן הדירוג שהעלים את כל התוצאות |
| יעדי מגע קטנים בסרגל הניווט | נסגר — לוגו 177×32, בורר שפה 46×30, מצב כהה 34×34, התראות 40×40, המבורגר 40×40 — כולם מתחת ל-44px, הסף של Apple/Google/WCAG. `minWidth`/`minHeight` בלבד: האייקונים לא השתנו, רק השטח סביבם. אומת בשני המשטחים: **0 קטנים** בדסקטופ ובנייד (היו 4 ו-5) |
| זיהוי אסמכתה (שכבה 3) | נסגר חלקית — `referenceIdentityService` מזהה קידומת ספק וכרטיס טיסה, ומצליב מול סוג הרשומה |
| התראות טיסה לאורח דיווחו הצלחה ולא היה להן נמען | נסגר — `/travel-info` פתוח בלי התחברות, ו-`savePushSubscription` דורשת `user.uid`. אורח לחץ, הדפדפן אישר, והמסך הכריז "ההתראות פעילות במכשיר הזה" מול שרת שלא ידע על קיום המכשיר. עכשיו: בלי משתמש אין כפתור אלא הסבר וקישור ל-`/login`; ועם משתמש, "פעיל" נגזר מ**השמירה** ולא מה-`subscribe` — ה-`.catch(() => {})` הריק הוחלף בביטול ההרשמה והודעה. אומת חי כאורח: `hasEnableButton:false`, `hasLoginGate:true` |
| מחיקת הזמנה בלחיצה אחת, בלי דרך חזרה | נסגר — `removeBooking` יוצרת סימון מחיקה, ולכן הסריקה הבאה מדלגת על ההזמנה בכוונה; הכפתור היה רביעי בשורה של ארבעה, מרחק נגיעה מחץ "הזז למטה". נוסף דיאלוג שאומר **מה** נמחק ומה המשמעות. אומת חי: לחיצה ⟵ 3 הזמנות נשארו 3; "ביטול" ⟵ 3; "מחק" ⟵ 2 והפריט ירד מהמסך |
| `passengers={2}` קבוע נכנס למכתב דרישה לחברת תעופה | נסגר — הערך לא היה מדידה של דבר: הוא הערך לדוגמה בסכמת ה-JSON שבפרומפט הפענוח. הוא הגיע למסך כסכום כסף וגם ל-`claimLetter`. `passengersOf` קוראת מההזמנה, ברירת מחדל **1** ולא ניחוש. אומת חי בשני הכיוונים: טיסה עם `passengers:4` ⟵ `(₪2,450 × 4 נוסעים)` = ₪9,800 (קודם ₪4,900, **חצי**); טיסה בלי השדה ⟵ €400 בלי שורת כפל (קודם €800, **כפול**). 17 מבחני נגד עברו, כולל `Number("")===0` ו-`Number([])===0` |
| "הדפס פרטי נסיעה" הדפיס את סרגלי הניווט ואת עצמו | נסגר — **וזו לא הייתה מחלקה חסרה אלא קובץ מת**: `.no-print` ו-`.print-only` מוגדרים ב-`src/assets/styles/style.css`, שאינו מיובא באף מקום. הקובץ שנטען הוא `src/style.css`, ובו אין `@media print` כלל. כלומר גיליון ההדפסה היה כתוב, לא נטען, ואיש לא ידע. הכללים הועברו ל-`index.css`. אומת חי: סרגל עליון, סרגל תחתון, בועת הצ'אט, כפתורי ההדפסה/ייבוא, האבחון והגרסה ⟵ `display:none`; הנסיעה, הציר והזכויות ⟵ נשארו; **ואקורדיון מקופל** (0px/hidden במסך) ⟵ 4673px/visible על הנייר |

## נקודת ההמשך המיידית — 07.09.2026

**דף הבית עבר סקירה מלאה ונסגר. המסך הבא מחכה.**

### מה עובד עכשיו, ושווה לדעת לפני שממשיכים

- **`ux-ui-reviewer`** (`.claude/agents/`) — סוכן ביקורת UI/UX. הוא מצא
  את רוב מה שתוקן היום. הפעל אותו על מסך **לפני** שנוגעים בו; הפלט
  שלו מסתיים ברשימת מדידות לביצוע, וזה החלק השימושי.
- **`fabricated-data-hunter`** — לערכים מומצאים.

### השיטה שעבדה, ולמה

סוכן קורא קוד ⟵ מדידה בדפדפן ⟵ תיקון ⟵ מדידה חוזרת. **הסוכן לבדו
לא מספיק**: הוא סימן ממצאים "דורש מדידה", ובכל אחד מהם המדידה שינתה
את המסקנה — לפעמים הפריכה אותה.

### מלכודות מדידה שנתפסו היום, ויחזרו

- regex על JSX נעצר על ה-`>` שבתוך `startIcon={<Icon />}` — החזיר 8
  במקום 54
- `animation: none` מנורמל בדפדפן ל-`animation: auto ease 0s 1 normal
  none running none` — חיפוש טקסט מפספס
- אריח לוויין מעל אוקיינוס חוזר 200 עם 678 בתים — הסטטוס אמר "עובד"
  והתוכן היה מים
- חלונית הדפדפן מדווחת `document.hidden: true` ואז **כל מדידת פריסה
  ב-JS מחזירה רוחב 0** — צילום מסך הוא המכשיר האמין שם
- קליק סינתטי (`el.click()`) אינו פותח popup של Leaflet ואינו מפעיל
  Enter — צריך קליק/מקש אמיתי דרך הדפדפן

### המסכים שטרם נסקרו

`/trip-planner` (מאחורי התחברות) · `/advanced-search` · `/my-trips` ·
`/journal` · `/rolling-trip` · `/matchmaker` · `/map` · `/group-trip`

### `/travel-info` — נסקר 07.09.2026, ארבעה תיקונים נחתו

הסוכן החזיר 17 ממצאים ו-15 מדידות. **ארבעה תוקנו** (ראה הטבלה למעלה);
מה שנותר פתוח מרוכז מייד אחרי. שלושה דברים שהסקירה לימדה, ויחזרו:

- **מחלקה שמוגדרת ואינה בשימוש נראית כמו תשתית קיימת.** `grep` על
  `.no-print` החזיר שתי תוצאות — שתיהן ההגדרה עצמה, אפס שימושים —
  והקובץ כולו לא מיובא. הכלל: לפני שמסתמכים על מחלקת CSS, לוודא
  שהקובץ שמכיל אותה **נטען**, לא רק שהוא קיים.
- **מדידה על אלמנט שאינו מרונדר אינה מפריכה דבר.** בדיקת מספר הנוסעים
  ב-5 שעות איחור החזירה "אין שורת כפל" — לא כי התיקון עבד, אלא כי
  LY381 היא TLV→FCO במוביל ישראלי, סף 8 שעות, ובלוק הזכאות כלל לא
  נוצר. רק 9 שעות הראו את הערך.
- **`@media print` אינו ניתן לאמולציה בחלונית.** מה שכן עובד: לשכפל את
  הגוש מתוך ה-CSSOM כ-`@media screen`, למדוד, ולהסיר. זה בודק את
  הסלקטורים וההצהרות — החלק שיכול להיות שגוי.

---

---

## מה נסגר לאחרונה

| נושא | מצב |
|---|---|
| כפילות בטיסות (`FCO` מול `ROME FIUMICINO`) | נסגר — `airportKey` ב-`bookingIdentity` |
| מיקומים ומפת היום | נסגר — כתובות מנוקות ב-`placeLookupService.addressVariants` |
| עריכת מסלול יומי (תיקון, הוספה, הזזה) | נסגר — שכבת `overrides`, חצים ↑↓ |
| קורלציה בין פרטי הנסיעה לתכנון | נסגר — `tripAnchorsService` + `DayAnchors` |
| תאריך ויום בכל יום בתכנון | נסגר |
| נקודת כניסה אחת להוספת פריט | נסגר |
| קישור לאישור המקורי במייל | נסגר — `messageId` נשמר, ואומת חי: 12 מ-13 הרשומות נושאות אותו והקישור מוצג |
| נסיעה שהסתיימה נשארה תחת "הנסיעות שלך" | נסגר — יורדת לקודמות ביום שאחרי |
| תמונת מקום שגויה תחת שם נכון | נסגר — `mustBePlace` ב-`placeMediaService`: פוסל דף פירושונים, ערך בלי קואורדינטות שהגיע מחיפוש, ולוגו |
| כותרת כהה על רקע כהה | נסגר — הוסר `color` מכלל `h1..h6` הגלובלי ב-`theme.css`; ארבעה מקרים בעמוד אחד |
| כפתור "אתר" באטרקציות + 66 אתרים חדשים | נסגר — אותו מקור כמו במסעדות (OSM), מ-3 ל-9 אטרקציות ליעד |
| שובר טרקלין בנה נסיעה בשם "יעד לא ידוע" | נסגר — קבוצה בלי מקום אינה מגדירה נסיעה (`tripGroupingService`) |
| "הנסיעות שלך (1)" ספרה פוליסה שפגה | נסגר — הספירה מונה נסיעות בלבד; אין נסיעה ⟵ "פריטים שדורשים טיפול" |
| כתובת השולח נזרקה לפני הפענוח | נסגר — `senderIdentityService`, ו-`sourceFrom` על כל רשומה |
| "הבא בתור" קרא הזמנות בלבד | נסגר — `plannedTimelineService`; **טרם נצפה על מסך אמיתי**, ראה פתוח 2 |
| "כפילות" הטיסות — הלופ שחזר שוב ושוב | נסגר — לא כפילות אלא ערך הפוך בפענוח. `bookingConsistencyService` בודק קוהרנטיות ולא נוכחות |
| ממשק מתורגם בפריסת RTL בכל השפות | נסגר — `dir`/`lang` הוחלו רק בהחלפת שפה, לא בטעינה; ו-`index.css` כפה `direction: rtl`. אומת: fr⟵ltr, he⟵rtl ללא רגרסיה |
| שלושה קישורי ניווט קשיחים בעברית מתוך 12 | נסגר — `nav.rollingTrip/journal/matchmaker` + "עוד" + תווית המשתמש, בחמש שפות |
| שמות היעדים בדף הבית בעברית בכל השפה | נסגר — `label` הופרד מ-`name` ב-`PlaceImage`; החיפוש נשאר עברי, 6/6 תמונות |
| שגיאת כתיב "מצ'קמייקר" | נסגר — `מאצ'מייקר`, אפס שאריות בעץ |
| `/trip-map` — 680 שורות נתוני סימולציה בלי קישור מאף מקום | נמחק. שני הרעיונות ששרדו — לוויין ופאנל עצירות — עברו למפה האמיתית |
| כותרת ה-Hero הבטיחה הדגמה שלא מרונדרת | נסגר — `titleGeneric` (קיים ומתורגם בחמש שפות, לא נקרא מעולם) מוצג כשיש נסיעה |
| ניגודיות ה-Hero נכשלה, אחרי שההדר תוקן ולא הוא | נסגר — `#667eea`⟵`#5568d3` ו-`#f093fb`⟵`#8e3fa8`. נמדד על המסך: הכותרת 3.66 ⟵ **5.19:1** |
| שתי אנימציות אינסופיות ב-`DemoItinerary` בלי שומר | נסגר — אומת בגיליונות: החץ וכפתור ההדגמה עוצרים ב-`prefers-reduced-motion` |
| שני כפתורים בדף הבית שאינם שייכים לו | נסגר — "טיול קבוצתי" היה כפילות של הסרגל + חומת התחברות לאורח; "מה לארוז" נפתח בלי ולו prop אחד |
| `/destination-info` — 482 שורות עברית, "לא מתורגם" | נסגר — הסקירה הראתה ש-385 מהן **מסד יעדים** ורק 16 ממשק. כל ה-16 + 11 שמות ערים; אפס עברית בצרפתית |
| הסרגל התחתון בנייד — 2 מתוך 5 פריטים קשיחים בעברית | נסגר — `BottomNav` לא נבדק עם הסרגל העליון. אותם מפתחות `nav.*`, לא עותק שני |
| תת-הכותרת החדשה הציפה את הנייד | נסגר — נוסח קצר ב-`xs`, גזור מהמלא. הצצה ‎-103 ⟵ ‎-32px, הכפתור עלה מ-531 ל-462 |
| אותו יעד נשא שני שמות: "פריז" בכרטיס, "Paris" בחלונית | נסגר — `label` מתורגם, `destination` נשאר אנגלי כי הוא הפרמטר בקישור |
| מצב כהה: 28 אלמנטים של לבן על לבן בדף הבית | נסגר — רקע קשיח + `color` מהערכה. נמדד: 28 כשלים ⟵ **0**, בשני המצבים |
| כותרת ה-Hero הוחלפה בהחלטת המשתמש, ותת-הכותרת הוחזרה | נסגר בדסקטופ — הנוסח החדש דחף את ה-Hero מעבר לקיפול (הצצה ‎-12px); אחרי קיצוץ גופן וריפוד: **66px**. **בנייד עדיין ‎-103px** |
| אין `h1` בדף | נסגר — `component="h1"` |
| תאריכי ISO גולמיים בכרטיס הנסיעה | נסגר — `fmtRange` לפי `i18n.language`, עם נפילה לערך הגולמי כשאינו ISO |
| כפתור שיתוף בתוך כרטיס לחיץ — Enter פתח דיאלוג **וגם** ניווט | נסגר — הופרדו לאחים; הניווט עבר ל-`<button>` אמיתי במקום `div[role=button]` עם `onKeyDown` ידני |
| ניווט מקלדת בלי סימון פוקוס בכל האפליקציה | נסגר — `index.css` הסיר `outline` מכל כפתור, ובכל העץ היו שני `:focus-visible`. נוספה טבעת גלובלית `#5568d3`; אומת בטאב אמיתי |
| 54 כפתורים ב-26 קבצים רונדרו לא כפי שנכתבו | נסגר — `style.css` דרס `sx` עם `!important` על `.MuiButton-containedPrimary`. ארבעת כללי ה-MUI שם נמחקו; `theme.js` כבר מגדיר את כולם, ושם הם ניתנים לדריסה |
| המפה הראתה יום אחד בלבד — נסיעה בין ערים לא נראתה כמסלול בשום מסך | נסגר — מצב "כל הנסיעה" ב-`TripMap`, מאותו `dailyItinerary`. אומת: פריז⟵בורדו, 6 סיכות ימים, 5 קטעים, 17 נקודות |
| אות פופולריות בעמוד החיפוש — הלולאה מתה אחרי 5 מקומות | נסגר — 429 מוויקיפדיה. בקשה אחת מרוכזת במקום 2 לכל מקום; 40 מקומות ב-362ms |
| ערך שגוי שנראה נכון: `"Pantheon"` ⟵ הפנתיאון של **פריז** | נסגר — `titles=`+`redirects=1` במקום `list=search`; ו-`coordinates` פוסל דף פירושונים ומונח גנרי |
| שתי תכונות בדף הבית לא עברו תרגום | נסגר — `home.features` הכיל 5 מפתחות מול 7 כרטיסים; 81 מפתחות נבדקו בחמש שפות |
| אטרקציה ריקה לגמרי נשמרה במאגר | נסגר — `activity` נוסף ל-`PURGEABLE` בהחלטת המשתמש; רשומה עם מספר אישור אינה נמחקת לעולם |
| אריחי מפה במצב לא־מקוון | נסגר — מטמון אריחים ב-`sw.js`, תקרת 500. אומת חי: אריח שנצפה מוגש עם `transferSize: 0` |
| הפירוט היומי היה תוכן ממלא מקום | נסגר — `generateItinerary` מזין את דף הבית ואת הטיול המתגלגל; 633 שורות של מערכים קשיחים ירדו |
| "מרחק: לא זמין" מעל מפה שמראה 347 ק"מ | נסגר — `roadRouteService` עם OSRM; נמדד 342 מול 347 של גוגל |
| ציון "התאמה לטיול" סתר את הטקסט לידו | נסגר — הציון הוסר; "מזג אוויר מושלם" מותנה בכך שאין ולו אזהרה אחת |
| שלוש לשוניות ריקות בכל יעד מובנה | נסגר — השלמה ברקע מאותו מקור AI, המידע הידני גובר, והחלק מסומן כאוטומטי |
| הוספת תמונה ביומן — גרירה והדבקה | נסגר — `addPhotoFiles` היא נקודת כניסה אחת לשלוש הדרכים (בחירה, גרירה, ⌘V); `resizeBase64` מחזירה `null` במקום להישאר תלויה על קובץ שאינו מפוענח (HEIC), וההודעה נגזרת ממה שקרה בפועל. אומת על המסך: 0→1 בהדבקה, קובץ טקסט נדחה, 7 תמונות מול מקום ל-4 נתנו "נוספו 4 · 3 מעבר לתקרה", ו-5 תמונות נשמרו ל-`journal_entries` |
| "כפתור התמונה ביומן המסע לא נותן לבחור" | **לא היה באג.** נמדד: הלחיצה מגיעה ל-`input` (`inputClicks:1`), אין כיסוי (`elementFromPoint` = הכפתור), והצנרת מייצרת `data:image/jpeg`. במאק התמונות סגורות בתוך `Photos Library`, שחלון בחירת קבצים אינו נכנס אליה — לכן נפתחת תיקייה ריקה למראה. הדלת היחידה היא **"מדיה ← תמונות"** בסרגל הצדדי. אומת: התמונה נוספה |
| שלושה כשלים שקטים | נסגר — כולם דפוס מס' 1, תוקן בסוג אחד ולא באחיו. **א׳** (`f3bbecb`): פענוח שנכשל נשמר במטמון כתוצאה ריקה ל-24 שעות; האח `aiAttractionsService` כבר זרק `INVALID_RESPONSE`. **ג׳** (`9573fe6`): `cloudError` יושם בשניים מחמישה נתיבי כתיבה; שלושת הנותרים חוברו, 4←7. **ב׳**: שגיאת פענוח הוצגה כ"נסרק ולא זוהו פרטי הזמנה" — זהה למייל שאין בו הזמנה; עכשיו מכסה, 5xx ו-`PARSE_FAILED` נאמרים בשמם, וכל ניסוח מסתיים ב"נסה שוב" כי כשל הוא "לא בדקנו" ולא "אין כאן" |
| מסלול על מפה אמיתית, לא iframe | נסגר — `RouteOnMap` מזין `path` מ-OSRM ל-`InteractiveMap` כ-Polyline עם סמנים. בדרך תוקנו שלושה: המפה ציירה רק באתחול ולכן מסלול שנשלף מהרשת לא הופיע; סמן בלי קטגוריה סוּנן בשקט; וזום קבוע (7) הבליע מסלול של 66 ק"מ. נמדד חי: תל אביב→ירושלים, 299 נקודות, זום 10 |
| המפה נכשלה בפרודקשן | נסגר — `RefererNotAllowedMapError`. המפתח ששימש היה **יתום**: לא נמצא באף אחד מחמשת הפרויקטים ב-Google Cloud. הוחלף ב-`Maps Platform API Key` שבפרויקט `Travel-Companion-Maps`, עם דומיינים מפורשים. **הלקח: הקונסולה באתר החי אמרה את הסיבה בשורה אחת, ואליה הגעתי אחרון** — שלושה סיבובים בוזבזו על `key=''` שמקורו ב-`RouteMap`, קוד מת שאיש אינו מרנדר |
| "הבא בתור" — נצפה על מסך אמיתי | נסגר — נזרעה נסיעה קרובה עם תוכנית פעילה ב-`/travel-info`. נמצא ותוקן באג: `nextFromBoth` מיין לפי `at`, וכניסה למלון (חצות) הקדימה טיסת 06:20. `DAY_ANCHOR` נותן לה 1441, אך `order` היה שובר-שוויון בלבד. אומת בשני הכיוונים |
| ניווט יצא מהמיקום הנוכחי במקום מנקודת המסלול | נסגר — `location=` אינו פרמטר ב-Maps URLs API; גוגל בולעת אותו ונופלת למיקום המכשיר. נוסף "נווט את כל המסלול" עם `origin` מפורש. נמדד: פירנצה→רומא 274 ק"מ במקום 4,102 ק"מ דרך טורקיה |
| האטרקציות והמסננים חזרו למפה (שלב 3) | נסגר — `AttractionsPanel` חובר ל-`/map` במקום ה-`iframe` של 25.2.2026. הפאנל מעולם לא רץ, וכשהורץ נמצאו חמישה כשלים: `useEffect` ירה לפני שסקריפט Maps נטען ולא ניסה שוב לעולם (זו הסיבה היחידה שנראה מת), כשל דווח כ"✅ נמצאו 0", קולבק שאינו נקרא השאיר ספינר נצחי, ותמונה ודירוג הונחו כקיימים — בקניות בפירנצה הם חסרים ב-10 ו-8 מתוך 20. `PlacesService` הישן **נבדק ועובד** עם המפתח הזה: 15 צירופים (3 ערים × 5 קטגוריות), כולם `OK`, ו-0 מתוך 150 תוצאות מסוג שגוי |
| "יש גרסה חדשה" נדבק לנצח ב-`localhost` | נסגר — `public/version.json` נכתב בידי craco בכל עלייה, ושרת פיתוח ובנייה לפרודקשן כותבים לאותו קובץ. `npm run build` בזמן ש-`npm start` רץ החליף את החותמת המוגשת בעוד ה-bundle בדפדפן נושא את הישנה. נמדד: `14:42:47` בקוד מול `16:12:02` ב-`/version.json`. בפיתוח הבדיקה כבויה — HMR ממילא מחליף את הקוד — ואומת מה-bundle הבנוי ש-`isDev` מקופל ל-`!1` ושהבדיקה החיה שלמה |
| 17 מסנני הקטגוריות עובדים שוב | נסגר — הסיבה בקוד: `onMapLoad` מוצהר ב-`App.js` ואינו נקרא מאף מקום. הוא היה היחיד שהציב `mapRef.current` ו-`isMapsLoaded`, וכשהמפה הוחלפה ב-`iframe` שניהם קפאו — כל צרכני `activeFilters` יוצאים בשומר שלהם. הקטגוריות עברו ל-`placeCategories.js` (מקור אמת אחד; ב-`App.js` הן הופיעו שלוש פעמים) ולפאנל ב-`/map`, כארבע קבוצות שהמשתמש עצמו חילק. נמדד בתל אביב: 50 · 79 · 96 · 79 שורות לארבע הקבוצות, כיבוי קטגוריה 79⟵20, כיבוי הכל ⟵ "לא נבחרה אף קטגוריה" |
| הבלוק הישן בדף הבית הוסר | נסגר — `App.js` מ-1,517 ל-121 שורות. ירדו 402 שורות JSX, שתים-עשרה פונקציות מתות (`searchRoute` לבדה 196 שורות), `useEffect` של הסמנים, 24 קריאות `useState`, `CATEGORY_ICONS`, וכ-60 שורות **JSX חשוף מחוץ לכל `return`** — הוראות סתמיות שהתפרשו ונזרקו, אך החזיקו ייבואים חיים ולכן נראו בשימוש. נמדד על דף הבית לפני ואחרי: 6,628px⟵3,013px, 71⟵28 כפתורים, 9⟵1 שדות, iframe אחד⟵0. שישה נתיבים נבדקו אחרי ההסרה, כולם מרנדרים, קונסולה נקייה |
| מזג האוויר היה מומצא | נסגר — שני משטחים הציגו קבוע: `weatherAPI.js` היה **סימולציה מוצהרת** שהחזירה 22° אחרי `setTimeout(500)` (השהיה מזויפת שגרמה לזה להיראות כקריאת רשת), ו-`DestinationInfoPage` נשא `{temperature: 22, description: 'בהיר'}` קשיח. נמדד על המסך: רומא, לונדון ובנגקוק הראו **22°C, בהיר** — זהה. שניהם עברו ל-`openMeteoService` (בלי מפתח), ונמדד אחרי: 26° · 19° · 26°. `weatherAPI.js` נמחק |
| מפתח OpenWeather — הפריט נסגר אחרת מהצפוי | שני המפתחות ב-`.env` **תקינים** (HTTP 200, ומבינים שמות עבריים). אבל סריקת 44 הצ'אנקים של האתר החי מצאה **אפס `appid=`** — כלומר לא הייתה בפרודקשן ולו קריאה אחת ל-OpenWeather. מה שהיה שם הוא הקבועים הקשיחים בלבד. Open-Meteo, שאינו דורש מפתח, כבר היה שם |
| תחזית 7 הימים ב-`/trip-planner` | נסגר — `getMockWeatherData()` השתמש ב-`Math.random()`: טמפרטורה 20–29 ומצב אקראי מארבעה, כלומר תחזית שהשתנתה בכל לחיצה. הוחלף ב-`fetchTripWeather`. בדרך נמצאו שלושה באגים שהיו בשירות מלכתחילה: `startDate` נשלח כאובייקט `Date` גולמי; `start_date` נשלח בלי `end_date` (400 — "must have the same number of elements") ו-`forecast_days` התנגש בו; ותאריך נבנה דרך UTC. **גם `getStopWeatherSummary` סבל מהם**, כלומר תחזית העצירות ב-`/rolling-trip` החזירה `null` בשקט |
| `ChunkLoadError` הרג את כל המסך | נסגר — כל דף הוא `React.lazy`, ולכן אחרי כל פריסה מי שהשאיר טאב פתוח מבקש צ'אנק בשם שכבר אינו קיים. `ErrorBoundary` הציג לו "Loading chunk ... failed" באנגלית ו"נסה שוב" ש**אינו יכול להצליח** — הוא רק מרנדר מחדש ומבקש את אותו קובץ. עכשיו: זיהוי `ChunkLoadError`, רענון יחיד עם דגל `sessionStorage` נגד לולאה, והודעה בעברית. אומת על מסך: רענון אחד ואז עצירה; שגיאה רגילה אינה נוגעת בזה |
| כלי Claude Code לפרויקט הותקנו | נסגר — MCP: `context7` (תיעוד ספריות חי, משרת את כלל "מחקר לפני פעולה") ו-`firebase` (קריאה ואימות של `firestore.rules`, ההגנה האמיתית היחידה); שניהם `✔ Connected`. סקילים: `/verify-live` (סורק 45 צ'אנקים, מפענח `\uXXXX`, עם `--self-test` דו-כיווני) ו-`/seed-bookings` (8 פיקסצ'רים בצורת הרשומה האמיתית). הוקים: אימות i18n אחרי עריכה, וחסימת `.env`. סוכן: `fabricated-data-hunter`. פקודה: `/deploy-check` |
| שני מפתחות OpenWeather ב-`.env` | נסגר — שתי השרשרות נמדדו מתות עד הסוף, ו-**שלושה** רכיבי מזג אוויר נמצאו במקום שניים. הוסרו: `travel-info/WeatherForecast.js` (559), `weatherService.js` (340), `maps/WeatherWidget.jsx` (346) והבלוק ב-`TripItineraryView`. `weather` ירד מ-`API_KEYS`, מ-`validateApiKeys` ומ-`API_ENDPOINTS` — **וגם משני ה-interfaces ב-`types/index.ts`, שבהם הוא היה שדה חובה**; הסרה מהאובייקט בלבד הייתה שוברת את הטיפוסים. אומת: build עבר, אפס הפניות תלויות באוויר, `/map` מצייר (`mapDrawn: true`, `innerWidth: 1280`) ו-`/travel-info` מרנדר בלי שגיאה חדשה. **הגינות: 0 `appid=` ב-45 הצ'אנקים אינה הוכחה — היא הייתה 0 גם לפני, כי הקוד כבר היה מת.** |
| `REACT_APP_OPENAI_API_KEY` — מפתח שלישי מת | נסגר — `API_KEYS.openai` היה **קבוע `''`**, ולכן `validateApiKeys` דיווח "Gemini חסר" תמיד; הפונקציה ממילא אינה נקראת. נמדד באתר החי: 0 מ-45 צ'אנקים מכילים `api.openai.com`. `openaiService.js` נשאר — **שמו מטעה, הוא קורא ל-`geminiEndpoint`** ואין בו מפתח. הביטול אצל OpenAI ומחיקה מ-Vercel בידי המשתמש |
| **ששה מתוך שבעה endpoints היו פתוחים לאינטרנט** | נסגר — נמדד חי: `POST` אנונימי ל-`/api/gemini` החזיר תשובה מלאה, `GET` ל-`/api/place-website` ביצע בקשה בתשלום, ו-`/api/notify-check` החזיר `projectId`, `users`, `flights`. המפתחות אכן עברו לשרת — אבל ה-endpoint שמחזיק אותם נשאר פתוח, ולכן המפתח לא נגנב אלא **הושאל**. דפוס מס' 1 שוב: `verifyIdToken` יושם ב-`push-test` בלבד, `CRON_SECRET` ב-`check-flights` בלבד. **אימות זהות היה שובר את האתר** — נמדד ש-Gemini נקרא מ-8 מסכים ציבוריים — ולכן נבנה `api/_lib/guard.mjs`, חסם מקור שבודק `Origin` **וגם** `Referer` (דפדפן אינו שולח `Origin` ב-GET מאותו מקור). אומת בשני הכיוונים על האתר החי: `curl`⟵403, דפדפן אמיתי⟵200. |
| תלויות צד-שלישי ו-OAuth | נסגר — `npm audit` החזיר 66, אבל המדידה שקבעה הייתה מי מהן **נארזת**. `jspdf` (CVSS 9.6) היה מיובא ב-`TripPlanner` עם **אפס שימושים** ובכל זאת נארז — ייבוא לבדו מספיק ל-webpack. `axios` היה תלות ישירה עם אפס אזכורים (28 ייעוצים). `react-router-dom` שודרג 7.13⟵7.18.3 בגלל open redirect ב-`<Link>`. 66⟵60, bundle 21MB⟵18MB. הנותרות אינן מגיעות לדפדפן: `websocket-driver` הקריטי מגיע מ-`@firebase/database` ש-tree-shaking משמיט — 0 מ-43 צ'אנקים. **OAuth נקי:** `gmail.readonly` בלבד, האסימון נשלח רק ל-`gmail.googleapis.com` ואינו נשמר כלל. |
| דף הבית — ששת הפריטים הראשונים מהביקורת | נסגר — נמדד לפני: **אפס** הפניות לנתוני המשתמש ב-`HomePage.js`, טענת "מעל 10,000 יעדים" מול 69 רשומות במאגר, 938px גלילה עד תוכן שימושי, 19 מתוך 35 יעדי מגע קטנים מ-44px, ותמונה **אחת** בכל הדף. נמדד אחרי: הדף 3488px⟵2269px (4.3⟵2.9 מסכים), יעדי מגע קטנים 19⟵6, שש תמונות ויקיפדיה אמיתיות. `NextUpCard` **לא שוכפל** אלא נקרא מ-`travel-info` — אותה פונקציה בשני המסכים. **הוא אינו נראה כרגע**, ובצדק: אין נסיעה עתידית בטווח, ואותה התנהגות נצפתה גם ב-`/travel-info`. |
| הזמנות מסעדה — מהמייל אל היום בתכנון | נסגר — שני שלבים. שלב 1 הוסיף את הסוג לפרומפט של `parseBookingEmail`, **שנקראת ממקום אחד בלבד: תיבת ההדבקה הידנית**. הסריקה החיה עוברת ב-`buildExtractionPrompt`, ושם עמדה ההוראה ההפוכה — "if the booking is for a restaurant table, set isBooking to false" — כלומר הצינור דחה במפורש כל מסעדה. שלב 2 מצא שמונה שומרים ולא שלושה; החשוב בהם `eventsFor`, שבו `restaurant` נפל ל-`default` ("ביטוח ומה שאין לו רגע בזמן") ולכן הרשומה נשמרה ולא הופיעה בשום יום. אומת בשתי הרסות על הצינור האמיתי, ואחת מהן הורצה מול הקוד שלפני השינוי והחזירה 0 אירועים. אומת גם על המסך עם `localStorage` מוזרע. **לא אומת:** הפענוח מול Gemini — דורש סריקה חיה |
| ארבעה באגים שנמצאו באימות מול התיבה האמיתית | נסגר — 05.09.2026, בהרשאת המשתמש. הפענוח עצמו עבד: גוגל ריזרב, TheFork ו-Tabit הוחזרו נכון, ושני ניוזלטרים נדחו. **(א)** Tabit החזיר `2024-08-16` במקום `2025`: הגוף כותב "16/8" בלי שנה, ו-`gmailService` שולף את תאריך המייל בעוד `bookingScanService` אינו מעביר אותו — בדיוק כמו `messageId` ו-`sourceFrom`. **(ב)** `EmailImportModal` החזיק מיפוי משלו ובו טיסות, רכב ומלון בלבד; אטרקציות, ביטוח ומסעדות נזרקו בשקט והמסך אמר "נמצאו ויובאו: ." — הצלחה עם רשימה ריקה. **(ג)** אותו שולחן בנאפולי הוזמן בשני ספקים והוצג פעמיים; הסיבה **אינה** הפסיק (`nameKey` מנקה אותו) אלא `startsWith` ב-`sameName` — הליבה יושבת בסוף כשהספק מקדים את סוג העסק. **(ד)** חמישה שולחים נוספו/הוחרגו, ו-Google Reserve חייב טבלת כתובות: `google.com` שולח גם התראות אבטחה וקבלות. אומת לפני/אחרי על אותו מייל: `2024-08-16`⟵`2025-08-16`, ו"נמצאו ויובאו: ."⟵"1 מסעדות" |
| סריקה חיה החזירה 15 מיילים ואפס הזמנות | נסגר — **היומן, לא הפענוח.** `PARSER_VERSION` עמד על 3 מאז schema.org, בעוד ההנחיות שוּנו שלוש פעמים (שלב 1, שלב 2, ותיקוני 05.09). `scanLedgerService` מדלג על כל מייל שכבר פוענח באותה גרסה, ולכן אישורי המסעדות שיושבים בתיבה לא הובאו כלל. אומת שהמנגנון חל: שלוש שורות הנושא עוברות את מסנן המילים, כלומר השאילתה **הייתה** מביאה אותן — ובכל זאת נסרקו 15 מתוך תקרה של 60. הועלה ל-4. **בדרך התגלה:** חמישה מהמיילים היו התראות TripIt שמצטטות את שורת הנושא של האישור האמיתי בלי פרטים — מצע לערך מומצא. `isEntitlementSender` הוכלל ל-`neverBookingSender`; לקרוא ל-TripIt "benefit" היה שקר בשם השדה. **טרם אומת:** שהסריקה הבאה מחזירה את המסעדות |
| סריקה שנפלה כולה בגלל מייל אחד | נסגר — הפענוח היה עטוף ב-`try`, אך הפסילה לפני המודל, בניית הרשומות והדחיפה לרשימה לא. חריגה אחת הפילה 60 מיילים והציגה שגיאה במקום תוצאה. גוף הלולאה עטוף כולו, והנופל נרשם ב-`unrecognized` ולא נבלע. אומת בהרסה התנהגותית: `gmailService` מוזרק דרך הוק של ה-loader, שלושה מיילים נכנסים ללולאה האמיתית ואחד זורק — לפני: קריסה מלאה; אחרי: `parsed=2`, שתי הזמנות, והנופל מדווח. **הסריקה החיה אחריה: 58 מיילים, 28 אישורים** (לפני: 15 ואפס) |
| שלושה באגים במסך שנמצאו כשהמשתמש ביקש "תבדוק" | נסגר — כולם על מסך אמיתי אחרי סריקה מוצלחת. **(א)** `gapMinutes` חישב מרגע ההמראה; שעת הנחיתה ישבה ב-`extra` ולא נכנסה. LY5111 15:00⟶17:15 הציגה "5 שעות ו-30 דקות" עד ארוחה ב-20:30 במקום 3:15 — וכך **כל** פער שמופיע אחרי טיסה. נוסף `freeAt`; כשהנחיתה אינה ידועה או חוצה חצות לא מוצג פער כלל. **(ב)** המודל החזיר את שם העסק בתוך שדה הכתובת, והשורה חזרה על הכותרת. ההסרה בתצוגה בלבד — הערך השמור נשאר מלא לאיתור על המפה. **(ג)** המפריד "יום אחד ללא הזמנות" נעלם: `skipped` חושב על `day.date` שהוא רגע האירוע הראשון ולא היום — 33 שעות התעגלו ל-1. הטעות דו-כיוונית (2.96 יממות ⟵ "יומיים"). עבר ל-`dayKey` עם `Date.UTC`. אומת לפני/אחרי על המסך בשני הכיוונים |
| בסיס הנתונים היה בלי גיבוי ובלי הגנת מחיקה | נסגר — נמדד 05.09.2026: `backupSchedules` ריק, PITR כבוי, delete protection כבוי, ושמירת גרסאות שעה אחת. כלומר מחיקה או קלקול = אין ממה לשחזר. **הופעלו:** `DELETE_PROTECTION_ENABLED` וגיבוי יומי בשמירת 7 ימים, שניהם אומתו בשאילתה חוזרת. **התיקון להנחה שלי:** חשבתי שדרוש שדרוג ל-Blaze — הפרויקט כבר עליו מ-02.03.2026; ה-`freeTier: true` הוא מאפיין של בסיס הנתונים (זכאות למכסת חינם) ולא של הפרויקט. הנפח: משתמש אחד, 14 הזמנות, 2 טיולים משותפים, פחות ממגה-בייט — עלות הגיבוי ~2 סנט לחודש לפי `$0.288` לג'יגה-חודש |
| ₪254 לחודש מ-Places Photo | נסגר — הדוח החי נקרא ב-05.09.2026: ₪48.55 בארבעה ימים, תחזית ₪253.98, ומתוכם **₪45.07 = 100% SKU בשם Places Photo** בפרויקט `Travel-Companion-Maps`, מסומן "New". המנגנון: `formatPlaces` ייצר כתובת תמונה לכל מקום ו-`AttractionsPanel` רינדר אווטאר בכל שורה — 4 קטגוריות × ~20 שורות = ~80 בקשות מחויבות בכל פתיחת מפה, בלי מטמון. **וזו התכונה שתיקנו**: הפאנל "מעולם לא רץ", ומרגע שחובר הוא רץ. תוך כדי התגלה ש-`photos` ו-`reviews` נשלפים בתשלום ב-`getPlaceDetails` ו**אינם נצרכים בשום מסך** — `PlaceGallery` יתום. התיעוד דו-משמעי לגבי רגע החיוב, ולכן הוסרו גם הרינדור וגם בקשת השדה |
| דף הבית לא ידע שיש לך נסיעה | נסגר — הפריט "אפס הפניות לנתוני המשתמש" היה מסומן נסגר, **ולא היה**: `NextUpCard` מחזיר `null` מחוץ ל-48 שעות (`HORIZON_MS`), ולכן נזרעה טיסה בעוד 35 ימים והדף לא השתנה בבית אחד (3,170px⟵3,186px, רעש). נוסף `UpcomingTripCard` עם אופק אחר — כל עוד הנסיעה לא הסתיימה — ובו ספירה לאחור, יעד, מה כבר סגור, וכמה ימים בלי תוכנית. **כל מספר נגזר; "חסרה טיסת חזור" ו"אין ביטוח" נדחו במכוון** — כיוון אחד הוא בחירה, ואזהרה שגויה אחת מלמדת להתעלם מכולן. וכשיש נסיעה קרובה הכותרת השיווקית מתכווצת (החיפוש נשאר). אומת: **אורח 3,170px⟵3,170px זהה בדיוק**, ועם נסיעה 3,186⟵3,226 — כרטיס מלא בעלות של 40px |
| האתר החי לא עלה — `Unexpected token '<'` | נסגר — ה-SW שמר `index.html` תחת כתובת של קובץ JS. **השרת היה תקין**: הצ'אנק החזיר 200, `application/javascript`, 1.36MB תקינים, וה-buildTime היה אחרי הקומיט. המנגנון: בזמן פריסה ה-SW הישן מבקש צ'אנק מהבנייה הקודמת, Vercel כבר החליף, והנתיב החסר מוחזר כ-`index.html` **עם סטטוס 200** (SPA rewrite). השומר היחיד היה `if (response.ok)` — ו-200 עובר אותו. מאותו רגע Cache First מגיש HTML לנצח, **ורענון אינו עוזר כי הוא עצמו עובר במטמון**. תוקן בשלושה מקומות: תשובת HTML לבקשת `script`/`style` אינה נשמרת ואינה מוגשת (504 מפורש), גם רשומה שכבר במטמון נבדקת, ו-`CACHE_NAME` v3⟵v4 כדי לנקות הרעלה קיימת. אומת חי: `trip-planner-v4` והשומר מופיעים ב-`sw.js` שעל השרת |
| דף הבית הסביר ולא הראה | נסגר — מבקר ראשון לא יכול היה להבדיל בין האפליקציה לכל מתכנן טיולים אחר, **והדבר שמייחד אותה — בניית המסלול מהמיילים — לא הופיע בדף כלל.** נוסף `DemoItinerary`: ארבעה "מיילים" מצד אחד, והמסלול שנבנה מהם מצד שני, **על המנוע האמיתי** (`buildTimeline` + `humanGap`) ולכן מציג "3 שעות ו-15 דקות" — הפער שתוקן באותו יום. הנתונים מסומנים כדוגמה בגוף הרכיב ואינם נוגעים במאגר. הכותרת הוחלפה בחמש שפות ל"ארבעה מיילים נכנסו. מסלול אחד יצא", והישנה נשמרה כ-`titleGeneric`. **שלוש איטרציות עד שזה תפס את העין:** מכווצת מתחת ל-Hero (נכשל — המשתמש דיווח שגם מי שמכיר איתר אותה רק אחרי כמה שניות), פתוחה בתוך ה-Hero, ולבסוף המיילים גלויים והלחיצה **בונה** את המסלול — כי הרגע שבו זה קורה מרשים מהתוצאה. ונסגר הפער האחרון: **"עכשיו בוא נעשה את זה עם התיבה שלך"** ⟵ `/travel-info?import=1` שפותח את חלונית הייבוא מעצמה. אומת: אורח 3,170px⟵3,209px, הכפתור מעל הקיפול, ואפס גלילה אופקית בנייד |
| שירותי תמונות מתים, ומספרים מומצאים בעמוד החיפוש | נסגר — המשתמש צילם כרטיסי אטרקציות עם תמונה שבורה. `source.unsplash.com` מחזיר **HTTP 503** (השירות נסגר), ו-`AdvancedSearchPage` בנה את שדה התמונה ממנו **תמיד** — כלומר כל כרטיס, תמיד. `picsum.photos` דווקא **חי (200)** ומגיש תצלום אקראי, וזה מסוכן יותר: הוא נראה כמו תצלום של המקום. נוקו כל ההפניות בקוד החי (14). **וחמור מכך נמצא בדרך:** `reviews: 1000 + i * 347` — מספרי הביקורות היו נוסחה על האינדקס, ושני המספרים בצילום (1,000 ו-1,347) הם שני האיברים הראשונים שלה. הדירוגים היו ברירות מחדל קשיחות. הוסרו משלושת הסוגים, **ובדרך נתפסה קריסה שהתיקון עצמו גרם**: `result.reviews.toLocaleString()` על שדה שנעלם, ומסנן הדירוג שהעלים את כל התוצאות |
| יעדי מגע קטנים בסרגל הניווט | נסגר — לוגו 177×32, בורר שפה 46×30, מצב כהה 34×34, התראות 40×40, המבורגר 40×40 — כולם מתחת ל-44px, הסף של Apple/Google/WCAG. `minWidth`/`minHeight` בלבד: האייקונים לא השתנו, רק השטח סביבם. אומת בשני המשטחים: **0 קטנים** בדסקטופ ובנייד (היו 4 ו-5) |
| זיהוי אסמכתה (שכבה 3) | נסגר חלקית — `referenceIdentityService` מזהה קידומת ספק וכרטיס טיסה, ומצליב מול סוג הרשומה |

## נקודת ההמשך המיידית — 07.09.2026

### ⏳ החלטת מוצר פתוחה — רב-לשוניות (ממתינה למשתמש, 06.09.2026)

**אל תסגור את זה בשקט ואל תמשיך לתקן i18n בלי הכרעה.** הממשק הראשי
מתורגם ומיושר נכון בחמש שפות, אבל שלוש שכבות נשארו עבריות בלבד
(`DemoItinerary`, שלושה עמודים שלמים, ו-138 `direction: rtl` קשיחים) —
הפירוט בטבלה תחת "מה פתוח".

השאלה שדורשת הכרעה: **האם האפליקציה באמת רב-לשונית, או עברית עם
בורר שפה?** התשובה משנה האם משקיעים בתרגום `buildTimeline`/`humanGap`
— שנוגע בליבת המוצר — או מקבלים את הרמה הנוכחית ומפסיקים.

---

**שתי בדיקות שממתינות למשתמש. אין פיצ'ר פתוח באמצע.**

- **החיוב** — לוודא שהוא נעצר. תוקן ב-05.09 (הוסרו כל בקשות
  `Places Photo`), והראיה מגיעה רק כשגוגל מעדכנת. לפתוח את
  [דוח החיוב](https://console.cloud.google.com/billing/014167-250978-E46121/reports)
  ולהסתכל על 6–7 בספטמבר. אם `Places Photo` באפס — התיקון עבד.
- **מסעדה בנסיעה עתידית** — שלב 2 אומת רק על נסיעה שכבר עברה.

---

## מה פתוח

### `/travel-info` — 13 ממצאים שנותרו מהסקירה של 07.09.2026

ארבעה תוקנו. אלה נמדדו או נקראו בקוד ולא טופלו, לפי סדר החומרה:

| # | ממצא | היכן | מצב |
|---|---|---|---|
| 1 | **שכבת התוכן כולה עברית קשיחה** — שמות ימים, תוויות אירועים, הצ׳יפים, `NextUpCard`, `FlightRights`, וכל הודעות `EmailImportModal`. משתמש צרפתי רואה `Importer depuis un email` ומקבל "נסרקו 12 מיילים" | `tripTimelineService.js:115-201`, `TripTimeline.jsx:28`, `TravelInfoComponent.js:263-269` | נקרא בקוד. כפוף להחלטת שפת התוכן שלמטה |
| 2 | **`dir="rtl"` קשיח** בשלושה דיאלוגים, סותר ישירות את `ThemeWrapper` שכבר מעביר `direction` לפי השפה | `EmailImportModal.js:215`, `AddPlanItemDialog.jsx:56`, `EventEditDialog.jsx:59` | נקרא בקוד |
| 3 | **מצב כהה — חמישה מוקדים של לבן מקובע.** ה-Paper החיצוני `#ffffff` והפנימי `background.paper`: כרטיס כהה בתוך כרטיס לבן. **נצפה בצילום מסך** | `TravelInfoPage.js:10`, `TripTimeline.jsx:115`, `DayMiniMap.jsx:110`, `TripBoundsReport.jsx:80` | חלקית נמדד |
| 4 | **אין מצב ריק.** למשתמש חדש כל הבלוקים מחזירים `null`; נשארים כותרת, שני כפתורים ותיבה מקווקוות. אין משפט שמסביר מה קורה כאן | `TravelInfoComponent.js` | נקרא בקוד |
| 5 | **"נסיעות קודמות" יושב מתחת לחותמת הגרסה** — הסימן האוניברסלי לסוף הדף | `TravelInfoComponent.js:517` מול `473` | נקרא בקוד |
| 6 | `TripBoundsReport` — כלי אבחון בזרימה הראשית. להתנות ב-`?debug=1`, או להציג רק כשיש בו `⚠` | `TravelInfoComponent.js:471` | החלטת מוצר |
| 7 | תאריכי ISO גולמיים: `2026-06-24 — 2026-06-30`. באותו קובץ כבר משתמשים ב-`toLocaleString('he-IL')` | `TravelInfoComponent.js:243` | נקרא בקוד |
| 8 | `FlightRights` פרוס במלואו לכל טיסה — הלוך-חזור = שני כרטיסים זהים. לקפל לשורה שנפתחת | `TravelInfoComponent.js:312` | החלטת מוצר |
| 9 | מקש רווח על `role="button"` פותח את הדיאלוג **וגם** מגלגל עמוד. חסר `e.preventDefault()` | `TravelInfoComponent.js:390` | נקרא בקוד, תיקון שורה אחת |
| 10 | ההערה טוענת ש-`NextUpCard` "בראש המסך"; בפועל לפניו חמישה בלוקים | `TravelInfoComponent.js:426` | נקרא בקוד |
| 11 | ארבעה קבצים מתים בתיקייה — `FlightInfo`, `CarRentalInfo`, `BookingDetails`, `AirportInfoModal`. אפס יבואים, ו-~25 מפתחות i18n שלהם חיים בחמש שפות | `src/components/travel-info/` | ניקוי |
| 12 | שתי כותרות כמעט זהות בזו אחר זו (`travelInfo.title` ואז `travelInfoPage.title`), ובאמצע תת-כותרת שמתארת את מה שרואים ממילא | `TravelInfoPage.js:20`, `TravelInfoComponent.js:329` | החלטת מוצר |
| 13 | **`<svg>` דקורטיביים בלי `aria-hidden`** — `TripTimeline.jsx:235`, `NextUpCard.jsx:81,136`, `DayMiniMap.jsx:116`. (16 כפתורי האייקון בציר **כן** קיבלו `aria-label` ב-07.09) | שם | נגישות |

**המדידות שטרם בוצעו** (מתוך 15 שהסוכן החזיר; אלה שלא נגעתי בהן):
יעדי מגע בציר ב-375px · יישור הקו האנכי מול מרכזי העיגולים · יישור
`GapRow` · **ניגודיות `NextUpCard` על הגרדיאנט** (`#764ba2` מול לבן הוא
~5.2:1 *לפני* `opacity: 0.75` — כמעט ודאי נופל) · גובה מודל הייבוא ב-
375×667 · שורת הצ׳יפים · חיתוך כותרת היעד · חיתוך תוויות `DayMiniMap`.

> ⚠️ **מלכודת מדידה שנתפסה כאן, ותחזור:** חלונית הדפדפן החזירה
> `center (0, 5253)` לאלמנט אמיתי — רוחב 0, אותה תקלה שכבר תועדה למעלה.
> קליק לפי `ref` נכשל, והדרך שעבדה היא ה-setter הילידי של `value` עם
> `dispatchEvent`. כל מדידת פריסה בשמונה המדידות שנותרו חשופה לזה,
> **וצילום מסך הוא המכשיר האמין שם.**

---

### 🚀 רשימת בדיקה להשקה — שפת התוכן

**מתי:** ממש לפני ההשקה, אחרי שהפרומפטים יציבים. **לא לפני.**
השפה היא השכבה האחרונה: כל שינוי פרומפט בהמשך היה מחייב לחזור על
העבודה הזו ועל סיכון המטמון שבה, שוב ושוב.

**מה הבעיה:** הממשק מתורגם לחמש שפות, אבל **התוכן שה-AI מייצר נעול
לעברית בפרומפט**. משתמש צרפתי מקבל ממשק צרפתי ותוכן עברי.

**שישה שירותים, כולם עם מטמון, אף אחד בלי גרסה או שפה במפתח:**

| שירות | "Hebrew" בפרומפט | מפתח המטמון |
|---|---|---|
| `aiDestinationService` | 8 | `dest_ai_` |
| `aiCarRentalService` | 5 | — |
| `rollingTripService` | 4 | `rolling_trip_` |
| `aiItineraryService` | 2 | `itinerary_ai_` |
| `aiHotelService` | 2 | — |
| `aiAttractionsService` | 2 | — |

**מה לעשות בכל אחד, באותו commit:**
1. להעביר את שפת הממשק לפרומפט במקום `Hebrew` הקבוע.
2. **להוסיף את השפה למפתח המטמון.** בלי זה, מי שמחליף שפה מקבל את
   התוכן הישן **שבעה ימים** ויחשוב שהתרגום שבור — בדיוק מה שקרה כאן
   עם `PARSER_VERSION`.

**התקדים כבר קיים בפרויקט, אין מה להמציא** — `placeMediaService.js:49`:

```js
const CACHE_VERSION = 'v2';
const raw = `${CACHE_VERSION}|${name}|${city}`.toLowerCase().trim();
```

**ומה שלא יישבר, וכדאי לדעת:** שם התצוגה ושם החיפוש **כבר מופרדים**
בארכיטקטורה. הפרומפט מבקש `"name"` להצגה ו-`"nameEn"` לחיפוש, ורק
`nameEn` מזין את ויקיפדיה ואת המפות. שינוי שפת התצוגה אינו נוגע בהם.
אותה הפרדה בדיוק נעשתה ב-`PlaceImage` (`label` מול `name`) וב-
`destination-info` (`slug` מול `name`).

**ולבסוף — עדות שנמדדה ופורשה שגוי, שלא תחזור:** Vercel Analytics
הראה 13 מבקרים, כולם מישראל, ב-31 יום. **זה אינו מדד לביקוש** —
האפליקציה טרם פורסמה. אין להסיק ממנו דבר על שפות.

## נקודת ההמשך המיידית — 06.09.2026

### ⏳ החלטת מוצר פתוחה — רב-לשוניות (ממתינה למשתמש, 06.09.2026)

**אל תסגור את זה בשקט ואל תמשיך לתקן i18n בלי הכרעה.** הממשק הראשי
מתורגם ומיושר נכון בחמש שפות, אבל שלוש שכבות נשארו עבריות בלבד
(`DemoItinerary`, שלושה עמודים שלמים, ו-138 `direction: rtl` קשיחים) —
הפירוט בטבלה תחת "מה פתוח".

השאלה שדורשת הכרעה: **האם האפליקציה באמת רב-לשונית, או עברית עם
בורר שפה?** התשובה משנה האם משקיעים בתרגום `buildTimeline`/`humanGap`
— שנוגע בליבת המוצר — או מקבלים את הרמה הנוכחית ומפסיקים.

---

**שתי בדיקות שממתינות למשתמש. אין פיצ'ר פתוח באמצע.**

- **החיוב** — לוודא שהוא נעצר. תוקן ב-05.09 (הוסרו כל בקשות
  `Places Photo`), והראיה מגיעה רק כשגוגל מעדכנת. לפתוח את
  [דוח החיוב](https://console.cloud.google.com/billing/014167-250978-E46121/reports)
  ולהסתכל על 6–7 בספטמבר. אם `Places Photo` באפס — התיקון עבד.
- **מסעדה בנסיעה עתידית** — שלב 2 אומת רק על נסיעה שכבר עברה.

---

## מה פתוח

**תוכן היעדים נוצר בעברית בפרומפט — זה המנוף האמיתי.**
`aiDestinationService.js:39` פותח ב-`"...for Israeli tourists. Use
Hebrew for all text values."`, ועוד ~10 מופעים של `"in Hebrew"`
בסכימה. כלומר משתמש צרפתי מקבל ממשק צרפתי **ותוכן יעד עברי**, ומסד
`destinationsData` (651 שורות) הוא רק גיבוי ל-11 ערים.

תרגום המסד לא יפתור כלום; שינוי הפרומפט כן. אבל יש מלכודת מתועדת:

```js
const CACHE_PREFIX = 'dest_ai_';        // שם היעד בלבד
const CACHE_TTL = 7 * 24 * 60 * 60 * 1000;
```

**אין שפה במפתח ואין גרסה.** שינוי הפרומפט בלי לטפל בזה = משתמש
צרפתי מקבל את התוכן העברי השמור, שבעה ימים — בדיוק הלקח של
`PARSER_VERSION`. כל שינוי כאן חייב להוסיף שפה למפתח **באותו commit**.




**רשימת האריזה — נסגר ב-07.09.2026.** בית אחד:
`PackingListModal`, על כפתור "רשימת ציוד" ב-`TripPlanner` — המקום
שבו המשתמש כבר מחפש אותה, ושבו יושבים `destination` ו-`tripDays`.
המימוש המקביל היה **קוד מת** (לא רונדר, פונקציות שלא נקראו) שכן
נשמר לתוך כל טיול; הוסר.


**מסכי המפה.** מצב "כל הנסיעה" נבנה ב-`TripMap` (06.09.2026) ומחובר
לציר לשני הכיוונים. ב-07.09 הוכרעו שתיים משלוש ההחלטות:

- ✅ **`/map`** — יש נסיעה פעילה ⟵ נפתחת מפת הנסיעה; אין ⟵ הסייר כפי
  שהיה. הסייר לא נלקח: כפתור "חיפוש יעדים" מחזיר אליו, וכפתור חזרה
  משם. עד כה המסך קרא **אפס** נתוני נסיעה ונחת על תל אביב הקשיחה.
- ✅ **`/route-map`** — שונה שמו ל"ניווט" בחמש שפות. הוא כלי לגיטימי
  (נקודה-לנקודה, ארבעה אופני תנועה), רק השם התנגש עם "מפה".
- ✅ **`/trip-map`** — נמחק (07.09). לפני המחיקה נבדק מה שווה להציל:
  "לוויין" ו"פאנל אטרקציות" עברו ל-`TripMap` על נתונים אמיתיים;
  "אטרקציות בסביבה" **לא** הועבר — זה הסייר, שנגיש בלחיצה מאותו מסך.


**i18n — מה שנשאר, ונמדד ב-06.09.2026.** הממשק הראשי מתורגם ומיושר
נכון בחמש שפות. שלוש שכבות עדיין עבריות בלבד, וכל אחת היא החלטה
בפני עצמה ולא תיקון:

| שכבה | היקף | הערה |
|---|---|---|
| `DemoItinerary` | 325 שורות, 0 קריאות `t()` | ההדגמה בראש הדף. מבקר צרפתי רואה אותה בעברית מלאה — והיא הדבר שאמור לשכנע אותו |
| `RollingTripPage`, `TravelJournalPage`, `DestinationMatchmakerPage` | ~350 מחרוזות, 8 קריאות `t()` בסך הכול | שלושה עמודים שמעולם לא עברו i18n |
| `direction: 'rtl'` / `textAlign: 'right'` קשיחים | 138 מופעים ב-16 קבצים | בעיקר דיאלוגים ווידג'טי צ'אט; נשארים RTL גם בצרפתית |

ומכשול אמיתי בשכבה הראשונה: `DemoItinerary` מרנדרת פלט של
`buildTimeline`/`humanGap` ("יום שבת, 10.10", "3 שעות ו-15 דקות"),
ואותן פונקציות מזינות גם את `TripTimeline`, `NextUpCard` ו-
`UpcomingTripCard` — כלומר מסכי הנסיעה האמיתיים. תרגום ההדגמה בלבד
משאיר אותה מעורבת, ותרגום מלא נוגע בליבת המוצר.


1. **הבלוק הישן ב-`App.js` הוא כעת כפילות מתה ודאית.** 17 המסננים
   שבו לא רק שאינם עובדים — הם קיימים עכשיו במקום שבו הם כן עובדים,
   `/map`. `onMapLoad` שם מוצהר ואינו נקרא, ולכן `isMapsLoaded` לעולם
   `false`. ההחלטה על הסרת הבלוק (פריט 9) הפכה קלה יותר: אין בו
   עוד דבר שאינו קיים במסך משלו.
   **חפיפה ידועה שנמדדה:** `nature` ו-`nationalPark` שולחים שניהם
   `park` ל-Places, ולכן כששניהם דלוקים כל השורות מתויגות "טבע".
   לבד, "פארקים לאומיים" מחזירה 20 שורות מתויגות נכון. ל-Places
   הישן אין סוג נפרד לפארק לאומי, ולכן ההבחנה אינה קיימת בנתונים —
   ולא הומצאה.
   **`EnhancedRouteMap.jsx` — לא להשתמש.** 66 שורות על `@react-google-maps/api`,
   ספרייה אחרת מזו שהמפה העובדת בנויה עליה, ועם `zoom={8}` קבוע — אותו באג
   שתוקן ב-`203e6cd`. מה שהוא נותן כבר קיים ועובד.

2. **תשתית Google מפוזרת על **שישה** פרויקטים, והחיוב פורק לגורמים.**
   נמדד 05.09.2026 מהתיבה: חשבון חיוב `014167-250978-E46121` ("My Billing
   Account"), Blaze על `my-trip-planner` מ-02.03.2026, וחיובים —
   אפריל ₪12.32 · מאי ₪1.00 · יוני ₪0.52 · אוגוסט ₪0.33 · **ספטמבר ₪16.27**.
   הקפיצה של ספטמבר משקפת שימוש של אוגוסט, החודש של עבודת המפות והסריקות.
   **נמדד בדוח החי (05.09.2026), לפי פרויקט:**
   `travel-companion-maps` **₪45.07** · `gen-lang-client-0886733868` ₪3.48 ·
   `my-trip-planner-3a72d` ₪0.00. קיימים גם `wine-app`, `wine-manager`
   ו-`my-project-1577426720080`; ארבעה מהם מקושרים לחשבון החיוב.
   **טופל:** מכסת `Requests per day` ב-Places API הישן ירדה
   מ-Unlimited ל-**1,000** (אומת בטבלה), והתקציב עלה מ-₪0 ל-**₪50**
   עם ספים 50/90/100/150% (אומת ברשימה: ₪48.55 / ₪50.00).
   **טרם אומת:** שהחיוב אכן נעצר — נתוני חיוב מתעדכנים תוך ~24 שעות.
   **ועוד מהתיבה:** ב-02.03.2026 נשלחו שתי התראות "Publicly Accessible Google
   API Key" — ל-`my-trip-planner-3a72d` ול-`gen-lang-client-0886733868`. זה
   מקור עבודת האבטחה, ושווה לשלול שהוא גם מקור חיוב.

3. **תשתית Google מפוזרת על שני פרויקטים** — Firebase ו-Gemini ב-`my-trip-planner-3a72d`,
   המפות ב-`Travel-Companion-Maps`. עובד, אך זה בדיוק מה שגרם לחיפוש של
   חמישה פרויקטים אחרי מפתח יתום. שווה לאחד בהזדמנות.

3. **שאלה למשתמש שטרם נענתה** — אילו פקדים בשני המסכים (פרטי הנסיעה,
   תכנון טיול) הוא בפועל אף פעם לא לוחץ עליהם. התשובה מכוונת מה להסיר.
   זו התגובה שסוכמה לחשש שלו שהאפליקציה נעשית מורכבת מדי.
4. **הקישור לאישור מפנה ל-`mail/u/0`** — כלומר לחשבון הגוגל הראשון
   בדפדפן; מי שמחובר לכמה חשבונות ייפתח לו החשבון הלא נכון. החלק השני
   של הפריט נסגר: אחרי `PARSER_VERSION: 2` נסרקו המיילים מחדש, 12 מ-13
   הרשומות נושאות `messageId`, ואומת חי שהקישור מוצג ומצביע להודעה
   הנכונה.
5. **רשומה ישנה אינה מתוקנת בדיעבד** — חסימת ייבוא מונעת רשומה חדשה
   ואינה מסירה קיימת. שובר הטרקלין שכבר במאגר נשאר שם, והוא הפסיק לבנות
   נסיעה רק משום שהתיקון בקיבוץ חל גם על נתונים שכבר נשמרו. **לקח: תיקון
   שנשען על סריקה בלבד לא יגיע למה שכבר במכשיר.**
6. **טבלת השולחים חלקית** — נבנתה מידע כללי, והחמיצה שולחים אמיתיים
   שהתגלו רק אחרי סריקה: אל על שולחת צ'ק-אין מ-`elal-check-in.com` ולא
   מ-`elal.co.il`. נוספו גם `simplebooking.it` ו-`discovercars.com`.
   8 מתוך 9 השולחים בתיבה מזוהים. יש להרחיב מול תיבה אמיתית, לא מזיכרון.
7. **שכבה 3 נסגרה חלקית** — `referenceIdentityService` מזהה קידומת ספק
   וכרטיס טיסה. אימות Luhn ואימות ספרת ביקורת נמדדו ולא שרדו, וזה מתועד
   כדי שלא ייבדקו שוב. הפירוט המלא בתחקיר "אנטומיה של מספר הזמנה":
   https://claude.ai/code/artifact/43bf75f4-e923-4d81-8960-a75c4a8d91c4
8. **עשרה רכיבים התייתמו עם הסרת הבלוק** — `PreferencesForm`,
   `HotelModal`, `EditAttractionModal`, `AccommodationList`, `ShareButtons`,
   `TripItineraryView`, `TravelAIChat`, `AIItineraryGenerator`,
   `DestinationInfo`, `TravelServicesTab`, `InviteButton`. אינם מיובאים
   עוד מאף מקום.
   **עודכן 04.09.2026:** `WeatherWidget` כבר אינו ברשימה — הוא נמחק עם
   סגירת פריט מפתחות מזג האוויר, יחד עם הבלוק שקרא לו ב-`TripItineraryView`.
   `TripItineraryView` עצמו נשאר יתום וממתין להחלטה כאן.
   **עודכן 05.09.2026:** נוסף יתום שנמצא במקרה בעבודת המסעדות —
   `src/components/travel-info/BookingDetails.js`. אף קובץ אינו מייבא
   אותו, והמסך החי מרנדר את `TripTimeline`. הוא נראה כמו המקום הנכון
   לתקן בו תצוגת הזמנה, וזו בדיוק הסכנה: תיקון שם אינו מגיע לאיש.
   **עודכן 05.09.2026 (ב):** `src/components/maps/PlaceGallery.jsx` — יתום
   תשיעי, והוא עלה כסף: `getPlaceDetails` ביקש `photos` בשבילו, השדה
   נשלף בתשלום, ואיש לא ראה תמונה. יתום שקט אינו בהכרח חינם.
   **טרם הוסרו: זו החלטה נפרדת, והמשתמש ביקש את הבלוק בלבד.**

9. **`ONE_PER_DOCUMENT` — מסעדה לא נוספה, במכוון.** הוספה שם אומרת
   שמייל אחד מאשר שולחן אחד לכל היותר. זה סביר לגוגל ריזרב ולטאביט,
   אך **לא נמדד**, ומסלול הזיהוי הכללי ממזג את המקרה ממילא (אותו סוג,
   אותו שם, תאריך שאינו סותר). מה שנופל בלעדיה: קריאה חוזרת של אותו
   מסמך אינה **מחליפה** רשומה קיימת, ולכן תיקון בפענוח לא יגיע לרשומה
   שכבר במכשיר. זה אותו פער של פריט 5.

10. **טיפים על עומס במקומות — חסום בהיעדר מקור אמין.** אין API פומבי
   ל-Popular Times, וגוגל אינה חושפת אותו. כל מספר עומס שנייצר יהיה
   המצאה שנראית כמדידה — בדיוק מה ש-`CLAUDE.md` אוסר, ובתחום שבו
   המשתמש מתכנן לפיו את היום. **מה שכן ניתן לבנות מנתונים אמיתיים:**
   אזהרת יום סגירה מתוך `tips.hours` שכבר קיים ומתוחזק ידנית ("מוזיאוני
   וותיקן סגורים ראשון"), שהיא עובדה ולא הערכה. דורש מבנה נתונים
   מסודר במקום טקסט חופשי, ב-11 היעדים.
   **מוצע: לא לבנות מדד עומס. לבנות אזהרת יום סגירה.**

## נתוני טיסות — סקירת שוק, אוגוסט 2026

**המטרה:** להציג את מה שמנועי החיפוש אינם מציגים — כבודה כלולה ותנאי
שינוי וביטול. **לא למכור**: המשתמש רואה את התמונה המלאה וממשיך לאתר של
חברת התעופה. כך אין רישוי, אין גביית כספים ואין תמיכה בכרטיסים.

בישראל, אגב, חובת רישיון לסוכנות נסיעות בוטלה ב-2002 — אך היא ממילא
אינה רלוונטית כל עוד לא מוכרים.

| ספק | מצב באוגוסט 2026 |
|---|---|
| Amadeus Self-Service | **בוטל ב-17.7.2026.** נשאר מסלול ארגוני עם חוזה |
| Kiwi Tequila | סגור מ-2024. בהזמנה בלבד, 50,000 משתמשים חודשיים |
| Travelpayouts | מחירים במטמון; חיפוש חי דורש 50,000 משתמשים |
| Aviationstack · AeroDataBox · FlightAPI | פתוחים, אך **סטטוס וזמנים בלבד** — אין כבודה ואין תנאים |
| **Duffel** | **הדלת היחידה שנותרה.** הרשמה עצמית, אסימון מיידי, ומחזיר גם כבודה וגם תנאי שינוי/ביטול |

**אזהרה למי שיקרא זאת בעתיד:** דפי התיעוד של Amadeus עדיין מתארים את
המסלול העצמי כאילו הוא קיים, וגם תוצאות חיפוש מפנות אליהם. רק דף
ההרשמה החי מודיע על הביטול. יש לאמת מול האתר, לא מול התיעוד.

**הפנייה מ-22.8.2026 כנראה מעולם לא נפתחה — נמדד ב-04.09.2026.** בתיבה
יש שני מיילים בלבד מ-`help@duffel.com`, שניהם "Please verify your email
address", ב-12:24 וב-12:26. **אין מייל יוצא, אין אישור פתיחת פנייה, אין
תשובה.** הקישורים הם `help.duffel.com/verification/anonymous_request/…` —
זרימת Zendesk שבה טופס שהוגש בלי התחברות **אינו נפתח כפנייה עד שהכתובת
מאומתת**. שתי הגשות תוך דקתיים מרמזות שכלום לא קרה בפעם הראשונה.
**רמת ביטחון:** ש-13 ימי השקט אינם דחייה — כמעט ודאי, אין רישום של פנייה.
שהסיבה היא קישור שלא נלחץ — הסקה מזרימת Zendesk, לא מדידה.
**הלקח, והוא כללי:** שקט מספק אינו נתון. לפני שמסיקים ממנו משהו, יש לוודא
שהשאלה בכלל הגיעה. כאן הוא תומחר כ"אין תשובה" במשך שבועיים.
**הדרך שנבחרה:** מייל ישיר ל-`help@duffel.com` — Zendesk פותח פנייה
אוטומטית ממייל נכנס, והכתובת מאומתת בעצם השליחה, בלי טופס ובלי קישור.
**נשלח 03.09.2026 22:40 UTC** ל-`help@duffel.com`, שרשור
`1a0696e363062d76`, אומת בתווית `SENT`. **ממתין לאישור פתיחת פנייה
מ-Zendesk — הוא הסימן שהפעם ההודעה נכנסה, וזה בדיוק מה שחסר ב-22.8.**
שתי השאלות המקוריות היו: האם ישראל היא מדינת התאגדות נתמכת, והאם מתקבל
עוסק יחיד. **נוספה שאלה שלישית וחזקה יותר, מתוך מחקר:** Duffel Payments
זמין ב-22 מדינות EU/UK ואין בהן ישראל — אך התיעוד ממסגר את ה-KYC סביב
*החזקת כספים*, ואנחנו איננו גובים. לכן נשאל אם ההגבלה בכלל חלה, ואם ניתן
טוקן `read-only` לחיפוש והצעות בלבד. `api/flight-offers.mjs` כבר בנוי ומחכה ל-DUFFEL_TOKEN; בלעדיו
הוא מחזיר רשימה ריקה ואינו שובר דבר.

**אם התשובה שלילית** — אין היום ספק חלופי שנותן את הנתונים האלה למפתח
יחיד. החלופה שנשארת היא לחלץ את אותם שדות מאישורי ההזמנה שכבר מגיעים
למייל של המשתמש: הם מכילים את הכבודה ואת תנאי הכרטיס, והם נתון שרק
בעל האפליקציה מחזיק.

---

## החלטות עקרוניות שאין לבטל בלי דיון

- **שני עורכים, קורא אחד.** הזמנות ותכנון נשארים נפרדים בבעלות
  ובעריכה; רק התצוגה מאחדת. הזמנה היא התחייבות ששולם עליה, פעילות
  מתוכננת היא כוונה, והצגתן כשוות מטעה לשני הכיוונים.
- **קריאה ולא העתקה.** התכנון קורא את ההזמנות בזמן התצוגה. העתקה יוצרת
  שני בעלים לאותה עובדה ונשברת בביטול: ההזמנה נמחקת וההעתק נשאר.
- **תיקון ידני נשמר ב-`overrides` ולא על השדה.** מייל האישור נסרק שוב
  ושוב; כתיבה על השדה הייתה נמחקת בסריקה הבאה, בשקט.
- **אין להמציא ערך.** מיקום משוער מסומן, שעה שאינה ידועה נשארת ריקה,
  ואזהרה מוצגת רק כשהיא ודאית. אזהרה שגויה אחת מלמדת להתעלם מכולן.

## מלכודות שחזרו ונשרפו עליהן

- **שירות חיצוני שמת אינו מודיע.** `source.unsplash.com` החזיר 503 בשקט,
  והאפליקציה הציגה תמונה שבורה בכל כרטיס חיפוש. **וגרוע ממנו — שירות
  שחי ומגיש אקראי:** `picsum.photos` מחזיר 200 ותצלום שנראה כמו המקום.
  תמונה שבורה מתגלה; תמונה שגויה מאמינים לה.
- **מספר שנגזר מהאינדקס נראה בדיוק כמו מדידה.** `reviews: 1000 + i*347`
  ייצר "1,000 ביקורות" ו-"1,347 ביקורות" — והמשתמש ראה אותם וחשב שהם
  אמיתיים. כשמסירים שדה מומצא, יש לבדוק **מי צורך אותו**: כאן ההסרה
  הפילה את מסנן הדירוג (`undefined >= 0` הוא false) והעלימה את כל
  התוצאות, וגם גרמה לקריסה ב-`.toLocaleString()`.

- **סטטוס 200 אינו אומר שקיבלת את מה שביקשת.** ב-SPA, נתיב חסר מוחזר
  כ-`index.html` עם 200. שמירה למטמון שנשענת על `response.ok` בלבד
  שומרת HTML תחת כתובת של JS, והדף נשבר ב-`Unexpected token '<'` —
  לנצח, כי גם הרענון עובר במטמון. **יש לבדוק את `content-type` מול
  `request.destination`, לא רק את הסטטוס.**
- **תיקון במטמון חייב לטפל גם במה שכבר שמור.** כלל שחל רק על תשובות
  רשת אינו מגיע למשתמש שההרעלה כבר יושבת אצלו. כאן נדרשו שלושה
  דברים: לחסום בכניסה, לבדוק גם בהגשה, ולהעלות את שם המטמון.

- **פריט שנסגר בהוספת רכיב שכמעט לעולם אינו נראה — לא נסגר.** "אפס
  הפניות לנתוני המשתמש" סומן נסגר כש-`NextUpCard` נוסף לדף הבית, והוא
  מוגבל ל-48 שעות. 99% מהזמן הדף של מי שיש לו שלוש הזמנות זהה לדף של
  מי שאין לו דבר. **הבדיקה שחושפת זאת היא לזרוע נתונים ולהשוות גובה
  לפני ואחרי** — לא לקרוא שהרכיב קיים ב-JSX.

- **תכונה שתוקנה היא תכונה שהתחילה לרוץ — ולעלות.** פאנל האטרקציות
  "מעולם לא רץ" עד שחובר ל-`/map`. מאותו רגע כל פתיחה שלו ייצרה ~80
  בקשות `Places Photo` מחויבות. החשבון קפץ ב-8,891% ו-**הדוח סימן את
  החיוב כ-"New"** — כלומר גוגל ידעה בדיוק מתי זה התחיל. לפני שמחברים
  משטח שנשען על API בתשלום, יש לשאול כמה קריאות הוא מייצר לכל צפייה.
- **שדה שנשלף ולא נצרך אינו חינם.** `photos` ו-`reviews` נתבקשו
  ב-`getPlaceDetails` ומעולם לא הוצגו: הרכיב שהיה אמור להציגם יתום.
  שניהם מעלים את דרגת החיוב. כשמסירים רכיב, יש לבדוק מה עוד נשלף
  בשבילו — יתום שקט יכול להמשיך לחייב שנה שלמה.

- **`innerText` מדלג על תוכן שאינו נראה.** מדידת הציר החזירה 519 תווים
  ו"אין ימים" בעוד ה-DOM הכיל את כל השורות — האקורדיון היה סגור.
  `textContent` קורא אותו. מכשיר שמחזיר ריק אינו מוכיח שאין שם דבר.
- **חישוב ימים על חותמות זמן אינו חישוב ימים.** `Math.round(Δms/יממה)`
  על רגעי אירוע נותן 1 ל-33 שעות ו-3 ל-71. יום קלנדרי נספר מ-`dayKey`
  ו-`Date.UTC`, אחרת מעבר שעון קיץ מחזיר את אותה טעות מהדלת האחורית.

- **באג רדום מתעורר כשמסירים את מה שהסתיר אותו.** `markProcessed`
  קיבל מחרוזת במקום מערך מאז `2ad4064`, ו-`'abc'.length` האמיתי העביר
  את השומר לפני ש-`forEach` קרס. הענף ירה רק על שוברי טרקלין, ואלה
  ישבו ביומן — ולכן מעולם לא רץ. העלאת `PARSER_VERSION` וחסימת TripIt
  נתנו לו חמישה מיילים, והסריקה כולה נפלה אצל המשתמש.
  **מי שפותח מסלול שלא רץ מעולם אחראי גם למה שמחכה בו.** שינוי שמרחיב
  קלט או מבטל מטמון הוא שינוי שמריץ קוד ישן בפעם הראשונה.
- **חתימה שקורסת על קלט סביר היא מלכודת, לא הגנה.** `(ids = [])`
  שמניח מערך תפס קורא אחד מתוך שניים. הנרמול נוסף בפונקציה עצמה, כי
  התיעוד שאומר "מקבל מערך" אינו רץ.

- **מטמון שנועד לחסוך כסף חוסם גם את התיקון.** `PARSER_VERSION` קיים
  בדיוק בשביל זה, והקובץ מתעד את הכלל בעצמו — ובכל זאת שוּנו ההנחיות
  שלוש פעמים ברצף בלי להעלותו. התוצאה נראתה כמו כישלון פענוח: סריקה
  שמחזירה אפס, בזמן שהאישורים יושבים בתיבה ועוברים את המסנן.
  **כל שינוי בפרומפט או במבנה השדות מחייב העלאת הגרסה באותו commit.**

- **תיבה אמיתית אומרת דברים שתיבה מדומיינת אינה אומרת.** אימות שלב
  המסעדות מול הדואר האמיתי (05.09.2026) העלה ארבעה באגים שאף בדיקה
  מקומית לא הייתה מגלה: שנה שהמודל המציא, מסלול הדבקה שזרק הזמנות
  בשקט, אותו שולחן שהוזמן בשני ספקים, וספק (TheFork) שלא היה בתמונה
  כלל. שלושה מהם אינם באגים של מסעדות — הם היו שם קודם.
- **ההשערה על סיבת באג היא ניחוש עד שמריצים אותה.** נקבע כאן ש"שתי
  הרשומות לא מוזגו בגלל הפסיק". `nameKey` מנקה פסיקים מזה זמן; הסיבה
  הייתה `startsWith`. חמש שורות של הרסה הפרידו בין השתיים. **לפני
  שכותבים סיבה בהודעת commit — להריץ אותה.**

- **פונקציה שנראית כמו הצינור אינה בהכרח הצינור.** שלב 1 של המסעדות
  תוקן ב-`parseBookingEmail` — שם משכנע, פרומפט מלא, שינוי שנבנה ועבר.
  היא נקראת ממקום אחד: תיבת הדבקה ידנית. הסריקה מהתיבה, כלומר המסלול
  שהמשתמש באמת מריץ, עוברת בפונקציה אחרת שבה עמדה ההוראה **ההפוכה**.
  לפני שמתקנים פרומפט או מפענח, יש לרוץ אחורה מהמסך אל הקוראים:
  `grep` על שם הפונקציה, ולוודא שהיא זו שהצינור נוגע בה.
- **סוג חדש חומק בשקט מכל רשימה מפורשת.** `restaurant` נבלע ב-`default`
  של `eventsFor` — הענף של "ביטוח ומה שאין לו רגע בזמן" — ולכן נשמר
  במאגר ולא הופיע בשום יום. אין שגיאה, אין אזהרה, יש היעדר. כשמוסיפים
  סוג: `grep` על סוג אח קיים (`'activity'`, `'transfer'`) בכל `src`,
  ולעבור על **כל** תוצאה. כאן זה נתן שמונה מקומות, לא שלושה.

- **מפתח שהועבר לשרת אינו מפתח שהוגן.** ההגנה שלמה רק כשגם ה-endpoint
  שמחזיק אותו יודע מי רשאי לקרוא לו. שישה כאלה ישבו פתוחים חודשים.
- **"נוסיף אימות זהות" היא התשובה המתבקשת, ולעיתים השגויה.** כאן היא
  הייתה מוחקת תכונות מכל מבקר שאינו מחובר. יש למדוד **מי קורא ל-endpoint
  ומאילו נתיבים ב-`routes.js`** לפני שבוחרים שומר.
- **ציון אבטחה שיורד אינו בהכרח נסיגה.** AgentShield ירד 98⟵94 כשנוספה
  רשימת `deny`, משום שקודם לכן הוא לא בדק את תוכנה כלל. המדד התחיל
  למדוד. אחרי השלמת הכללים: 99.
- **הערה יכולה לשרוד מזעור, ולתת חיוב שגוי.** התיעוד מזהיר שהערה נמחקת
  ולכן חיפוש שלה מחזיר שלילה שגויה. ב-04.09.2026 קרה ההפך: הערה שנכתבה
  בעברית שרדה בבניית Vercel (אך לא בבנייה מקומית), וחיפוש `jsPDF` "מצא"
  ספרייה שכבר הוסרה. יש לחפש **סמן ייחודי לקוד הספרייה** (`jsPDF.API`,
  `addFont`) ולא מחרוזת שעשויה להופיע בטקסט שכתבנו.
- **`npm audit` סופר חבילות, לא סיכון.** 66 ממצאים, ומהם 27 קריטי/גבוה
  היו כלי בנייה שאינם מגיעים לאיש. השאלה היחידה היא מה **נארז ל-bundle**
  או רץ ב-serverless — ואותה בודקים בבנייה, לא ברשימה.
- **צילום מסך יחיד אינו מדידה.** ב-04.09.2026 הוכרז כאן "הרעתי" על סמך צילום
  שהראה כרטיסי יעד ריקים; הצילום נתפס באמצע טעינת התמונות, וצילום שני הראה
  שש תמונות אמיתיות. יש לצלם שוב לפני שמסיקים, בדיוק כמו בכל מכשיר אחר.
- **שומר חד-כיווני נראה מוצלח.** יש לבדוק גם שהבקשה שלנו **עוברת**;
  שומר שחוסם את כולם עובר בהצטיינות בדיקה שמוודאת רק חסימה.

- **`toISOString`** — מחזיר UTC. חצות מקומית בישראל היא אתמול. הופיעה
  ארבע פעמים ביום אחד. תאריך נבנה תמיד מרכיבי הזמן המקומיים.
- **ערך שנגזר בשני מקומות** — הציר כיבד את `overrides` והקיבוץ לא,
  ואותה רשומה ישבה בשני תאריכים בלי ששום דבר ייכשל.
- **לחיצות מהירות שנפתרות נכון אינן הוכחה שאין מרוץ.** שלושה שבבים
  שכובו ברצף נתנו תוצאה נכונה — משום ש-React אחד אותם לעדכון אחד,
  ולא משום שהקוד היה בטוח. המרוץ האמיתי נחשף בהחלפת לשונית באמצע
  חיפוש של חמש בקשות. נוסף מונה בקשות, ואומת: אחרי מעבר "תרבות"⟵"טבע"
  באמצע טעינה, כל השורות שייכות לקבוצה האחרונה בלבד.
- **`grep --include` נופל בשקט ב-zsh.** `--include=*.js` בלי ציטוט
  מפיק `no matches found`, ה-grep רץ בלי המסנן, וכל 18 הרכיבים דווחו
  כ"יתומים". תשובה אחידה וחשודה היא סימן למכשיר שבור, לא לממצא.
- **כלי גיזום אוטומטי גזם את הרכיב עצמו.** סקריפט שהסיר הצהרות לפי
  איזון סוגריים בלע את `return` הראשי, ו-`App.js` ירד ל-197 שורות עם
  `React` ו-`AppRoutes` מסומנים כלא בשימוש. `git checkout` והסרה ידנית
  בטווחים מאומתים, שורה-שורה, עלו פחות מהניסיון לתקן את הכלי.
- **"תתחבר ותראה" היה מוביל לדיווח שקרי.** הרכיב שתוקן, `WeatherForecast`,
  **מיובא ואינו מרונדר** ב-`TripPlanner`; מה שהמסך מציג בפועל הוא בלוק
  פנימי אחר. התחברות הייתה מראה נתוני mock ומאשרת תיקון שלא נגע במסך.
  פיגום זמני בנתיב ציבורי מדד את הדבר עצמו, בלי סיסמה.
- **תחזית שנשארת אחרי שהיעד השתנה.** הכפתור `disabled={showWeatherForecast}`
  ננעל אחרי לחיצה אחת, ולכן מספרי רומא נשארו על המסך תחת הכותרת
  "תחזית מזג אוויר ב<היעד החדש>". נתפס רק משום שהבדיקה הנגדית נכשלה.
- **שם עברי אינו נמצא בגיאוקוד באנגלית — בפעם השנייה.** `geocodeCity`
  שאל `language=en`, ו-18 ערים נמדדו: 8 בלבד נמצאו, וכל שם עברי נכשל.
  `language=he` נתן 18/18 — אך `"New York"` נפתר ל-40.9,-97.6, שהיא
  נברסקה. פגיעה שגויה אינה נספרת בשום מונה הצלחות. הכלל שנבחר: השפה
  נגזרת מכתב השם. זהו בדיוק סיבוב 1 בעבודת תמונות המקומות, שחזר.
- **"הבאנר נעלם" אינו מדידה.** `watchForUpdates` יוצא בשקט כשהלשונית
  אינה בחזית, ולכן בפאנל מוסתר הבאנר לא מופיע בין אם התיקון עבד ובין
  אם לא — ובדיקה נגדית שאמורה הייתה להחזיר אותו נכשלה משום שהמכשיר
  היה שבור, לא הקוד. נוסף `window.__updateReport()`: חותמת הקוד, חותמת
  השרת, מצב הלשונית ו-`wouldShowBanner` — כולם בשורה אחת.
- **פאנל דפדפן מוסתר מודד אפס.** `document.hidden` הוא `true`, הדף נפרס
  ברוחב 0, ו-`__mapReport` החזיר `containerSize: 0×0` ו-`.gm-style: 0` —
  כלומר "המפה לא צוירה". היא כן צוירה: אחרי הצפת הלשונית אותה בדיקה
  נתנה 767×466 ו-21 אריחים. שלושה סיבובים הלכו על תיאוריות למה המפה
  שבורה, לפני שנבדק שהמכשיר תקין.
- **מפתח JSON שנוסף לבלוק הלא נכון נראה כמו תרגום חסר.** `"map"` מופיע
  פעמיים ב-`he.json`; ההוספה נחתה בבלוק המקונן והמסך הציג
  `map.geocodeFailed` כטקסט. יש לאמת ב-`json.load` שהמפתח יושב במקום
  שממנו קוראים, ולא בהתאמת טקסט ראשונה.
- **פונקציה שנבדקה בבידוד בעוד החיווט שלה לא** — מעצב התאריך עבר את כל
  הבדיקות בזמן שהמסך היה ריק, כי איש לא קרא לו עם נתון אמיתי.

---

## הפעלה ובדיקה

**תיקיית העבודה היא `~/Developer/my-trip-planner`.** היא הועברה לשם
ב-27.8.2026 משולחן העבודה, שמסונכרן ל-iCloud ורוקן את תוכן הקבצים
(`dataless`) — מה שהפיל את הזיכרון ומנע מ-`npm start` לעלות. הפירוט
ב-`.claude/memory/project-location.md`. אין להחזיר לשולחן העבודה.

```bash
npm start                 # http://localhost:3000
```

`/travel-info` נגיש בלי התחברות ונטען מ-`localStorage.importedBookings`,
ולכן אפשר לזרוע נתונים ולבדוק את המסך האמיתי. `/trip-planner` מוגן
בהתחברות — כשלא ניתן להריץ אותו, יש לומר זאת במפורש בדיווח.

כלי אבחון שנפרסים: `window.__dedupeReport()`, "אבחון: למה נסיעה מכילה
את מה שהיא מכילה" בתחתית `/travel-info`, `/api/notify-check`, כפתור
"שלח בדיקה", וחותמת הבנייה במסך.

## גישה למשתמשים נוספים

ההרשמה פתוחה — אין צורך להוסיף אף אחד ידנית, ו-`firestore.rules` מבודד
כל משתמש לנתוניו בלבד. **חריג אחד:** סריקת המייל דורשת `gmail.readonly`,
ובמצב Testing של מסך ההסכמה בגוגל היא זמינה רק למי שרשום כ-Test User —
עד 100 משתמשים, וההרשאה פגה כל 7 ימים. מעבר לכך נדרש אימות של גוגל,
שהוא הליך ארוך משום שזהו scope מוגבל.
