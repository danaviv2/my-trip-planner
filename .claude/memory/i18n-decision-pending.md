---
name: i18n-decision-pending
description: החלטת מוצר פתוחה — האם האפליקציה רב-לשונית באמת, או עברית עם בורר שפה
metadata:
  type: project
---

ב-06.09.2026 תוקנו: פריסת RTL שנכפתה על כל השפות, סרגל הניווט, כרטיסי
דף הבית ושמות היעדים. הממשק הראשי מתורגם ומיושר נכון בחמש שפות.

**נשאר פתוח, במפורש בבקשת המשתמש שלא יישכח:** שלוש שכבות עבריות בלבד —
`DemoItinerary` (325 שורות, 0 קריאות `t()`), שלושה עמודים שלמים
(מתגלגל, יומן, מאצ'מייקר — ~350 מחרוזות), ו-138 `direction: rtl` קשיחים
ב-16 קבצים.

**Why:** תרגום ההדגמה חסום על `buildTimeline`/`humanGap`, שמזינות גם את
`TripTimeline`, `NextUpCard` ו-`UpcomingTripCard` — כלומר מסכי הנסיעה
האמיתיים. זו החלטת מוצר על היקף, לא תיקון באג.

**How to apply:** לא לפתוח את זה מחדש בלי הכרעה מפורשת של המשתמש, ולא
לתקן i18n נוסף בטפטופים. להעלות את השאלה כשחוזרים לנושא. הפירוט המלא
ב-`STATUS.md`, בראש הקובץ ובטבלת "מה פתוח".
